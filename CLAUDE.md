# CLAUDE.md

给 Claude Code 的项目导航。SullyOS 是装在浏览器里的虚拟手机系统（React + TS + Vite，local-first，IndexedDB 存储）。详细介绍见 [`README.md`](./README.md)。

这份文件只做一件事：**告诉你遇到某类问题该去翻哪份文档**，别在代码里瞎逛。

> 包管理器统一用 **pnpm**：装依赖 `pnpm install`、跑测试 `pnpm vitest run`、跑脚本 `pnpm <script>`。别用 npm / yarn（仓库里是 `pnpm-lock.yaml`）。

## 文档地图

| 主题 | 文档 | 什么时候看 |
|------|------|-----------|
| **开发调试面板 / 开关** | [`docs/dev-debug.md`](./docs/dev-debug.md) | 加 dev-only 开关、加调试日志、排查"角色怎么又不说话了"。含逐步指南 |
| **记忆系统** | [`docs/memory-system-overview.md`](./docs/memory-system-overview.md) | 涉及长期记忆、月度总结、向量化记忆宫殿、情感空间。改记忆相关逻辑前必读 |
| **查手机 · 人际关系系统** | [`docs/relationship-system.md`](./docs/relationship-system.md) | 改「查手机」聊天/通讯录、角色联系人/好感、真假甄别、真角色双向对话、虚构 NPC 约束前必读 |
| **见面 · 观测协议 OBSERVE** | [`docs/date-observe.md`](./docs/date-observe.md) | 改见面（DateApp）的角色观测面板：提示词注入、掉格式解析容错（两层）、全息 HUD 渲染前必读 |
| **彼方 · 信号坠落处（跨用户接龙诗）** | [`docs/signal-poetry.md`](./docs/signal-poetry.md) | 改彼方(VRWorld)「信号坠落处」房间：跨实例合写现代诗、复用漂流瓶后端、`po_poems`/`po_poem_lines` 表与 `/poem/*` 端点、两层容错解析、并发安全前必读 |
| **捏人器 PSD 导入 / 部件投影层** | [`docs/char-creator-psd-import.md`](./docs/char-creator-psd-import.md) | 改捏人器素材管线、部件阴影（正片叠底预转）、PSD 图层组约定前必读 |
| **QQ捏人工坊（神经链接手办柜）** | [`docs/chibi-studio.md`](./docs/chibi-studio.md) | 改小小窝/彼方/520 三处 Q 版形象、捏人器 savedState 还原、`chibiStudio` 字段前必读 |
| **角色自定义时区** | [`docs/character-timezone.md`](./docs/character-timezone.md) | **写任何跟时间有关的代码前先扫一眼**：prompt 里的「现在是」、角色作息/夜间判断、日期 key、界面上的钟。分清「角色那边几点」和「用户自己的时间」，别自己手搓时差。文末列了还没接时区的几处（主动消息 + 几块界面上的钟），**正式发版前记得过一遍** |
| **通用 MCP 工具服务器** | [`docs/mcp-client.md`](./docs/mcp-client.md)（开发者）、[`docs/mcp-user-guide.md`](./docs/mcp-user-guide.md)（用户教程，设置「?」弹窗跳转的就是它，改接入行为要同步） | 改用户自配 MCP 接入（设置板块、握手/session、工具循环、`?target=` 代理约定、worker/mcp-proxy）或排查「工具连不上/角色不调工具」前必读；主动消息 2.0 的后台 MCP 路径（配置上云 / fire 时注入 / worker 直连执行）也在这份 |
| **主动消息 2.0 · 即时对话** | [`plans/amsg2-instant-chat.md`](./plans/amsg2-instant-chat.md)（设计与取舍）、[`plans/amsg2-instant-chat-contract.md`](./plans/amsg2-instant-chat-contract.md)(端点/信封/fire_pack v7 契约) | 改「聊天在用户自己的 CF Worker 上生成」这条路（`POST /instant-chat`、`utils/amsgInstantChat.ts`、fire_pack 的 `chat` 段、chat_outbox 补收、「正在输入」超时）前必读 |
| **主动消息 2.0 · 后台任务（不说话的活儿）** | [`plans/amsg2-expansion.md`](./plans/amsg2-expansion.md) | 改「页面关着也能跑完」的后台活儿前必读：`metadata.amsgKind` → handler 注册表（`worker/amsg/src/fireKinds.ts`）、一次性输入的 `amsg:job` 命名空间与 TTL、`ctx.emitResult` 的结果回程与客户端分发（`utils/amsgResults.ts`）。文首「现状」是实况，正文是「还有哪些调用点值得搬、哪些不该搬」的取舍 |
| **主动消息 2.0 · API 凭据引用 credRefs** | [`plans/amsg2-llm-credentials-contract.md`](./plans/amsg2-llm-credentials-contract.md) | 改凭据上云（`llm_credentials` 表、任务 `credRefs`、`utils/amsgLlmCredentials.ts` 的每角色三行）或排查「换 Key 后主动消息 401 / 不来了」前必读；文末「SullyOS 侧落地」是实况 |
| **Instant Push SSE↔Push 契约** | [`docs/instant-push-dual-channel.md`](./docs/instant-push-dual-channel.md) | **改 instant push 路径或排查「报错但收到消息」类 bug 前必读**。SSE ≠ 送达判定通道、catch 不能直接判 send-failed |
| **Instant Push 通道** | [`docs/instant-push-branch-notes.md`](./docs/instant-push-branch-notes.md)、[`worker/instant-push/README.md`](./worker/instant-push/README.md) | LLM-driven Web Push、worker 端 agentic loop / reasoning / 副作用 directive |
| **使用统计** | [`docs/analytics.md`](./docs/analytics.md) | **加任何埋点前必读**。收什么/不收什么的边界、事件名与属性的规矩（属性只能是固定枚举）、构建时门禁与开关、完整事件清单。想加「某功能有多少人开了」看「加新埋点的规矩」第 5 条，别在配置页现场发 |
| **二改 / 加 App / 数据流 / 后端 Worker** | [`README.md`](./README.md) 「给想二改的人」一节 | 新增 App、build badge、sfworker 代理替换、开源协议 |

> README 的「给想二改的人」区域信息量很大（数据流、ContextBuilder、Instant Push Phase 2、sfworker 清单），动后端 / 加功能前先扫一遍。

## 发版前改一下版本号

[`utils/buildInfo.ts`](./utils/buildInfo.ts) 里的 `APP_VERSION`（形如 `v3.0 (Ambient Presence)`）是手工维护的，**做完一轮大功能或者性能优化就改一下**。若只是小修复则不用提起。

它有两个用处：设置页底部显示的就是它；统计还拿版本号那半截当标签，面板按它切分数据。不改的话新旧版本的数字堆在同一个标签下，「这次优化有没有让首屏变快」「新版铺开多少了」就都答不出来。括号里的代号只在界面上显示，不进标签。构建 hash（`BUILD_LABEL`）是自动生成的，不用管。

---

# Translation Tracking — Chinese → English UI (added by this session, not part of upstream docs above)

## Project

SullyOS ("aetheros-simulator") — a browser-based "virtual phone OS" for AI-companion simulation. Stack: React + TypeScript + Vite, local-first with IndexedDB storage, pnpm workspace, optional Cloudflare Workers backend (`worker/`) and Capacitor mobile build. Fork of `qegj567-cloud/SullyOS`, now at the user's own `https://github.com/arvelaau/SullyOS` (this working copy is a **shallow clone**, `--depth 1`, of that fork — no older history available locally).

Baseline commit: `c1461dbf09459ab3e2c1cd4dfdff6030d9e43d0b` (`c1461db`), branch `master`, working tree clean at clone time — this is the reference point for all `git diff` going forward.

Repo layout (top-level, relevant to translation):
- `apps/` — top-level screen/app components (`Chat.tsx`, `Settings.tsx`, `VRWorldApp.tsx`, `MemoryPalaceApp.tsx`, `CallApp.tsx`, `DateApp.tsx`, `GroupChat.tsx`, `GameApp.tsx`, etc.) — the actual phone "apps."
- `components/` — shared UI + per-feature subfolders (`appearance/`, `bank/`, `call/`, `character/`, `chat/`, `date/`, `handbook/`, `journal/`, `lifeRecord/`, `mcd/`, `novel/`, `os/`, `schedule/`, `settings/`, `song/`, `user/`, `voice/`, ...).
- `utils/` — **not just helpers**: this is where nearly all AI prompt-construction and protocol-parsing logic lives, flat (no `prompts/`/`ai/`/`llm/` subfolder convention), plus per-feature prompt files nested under `utils/groupChat/`, `utils/like520/`, `utils/memoryPalace/`, `utils/vrWorld/`, `utils/worldHome/`.
- `docs/` — upstream's own dev-doc map (see doc table above this section) — several entries describe AI-prompt/protocol systems directly and were used for this scouting pass.
- `plans/` — design docs for the "proactive message 2.0" (主动消息 2.0 / amsg2) subsystem.
- `worker/` — Cloudflare Workers backend (post-office, instant-push, mcp-proxy, amsg job runner) — server-side counterpart to some client-side protocols (e.g. `worker/post-office/src/index.ts` implements `/poem/*` matching `utils/vrWorld/prompts.ts`'s `parseSignalOutput`). Out of scope question, see Scope note.
- `更新日志/` — literally "changelog" as a folder name; not code, not part of this pass unless asked.

## Two categories of Chinese text

**Standing rule on code comments (`//` and `/* */`), decided 2026-09-03**: translate them too, same as UI text — they're not exempt just because they're never rendered. This had drifted inconsistently across earlier batches (some translated comments, e.g. `MessageItem.tsx`, `ChatConstants.ts`, `StoryTheaterSession.tsx`, `RoomPlatePanel.tsx`; some explicitly left them all in Chinese, e.g. `ActiveMsgGlobalSettingsModal.tsx`, `apps/VRWorldApp.tsx`'s first pass) because the two-category framework below only ever classified rendered UI text vs. AI-prompt text, never addressed developer-facing comments explicitly. User decision: translate everywhere, for future-maintainer consistency. The stragglers from before this decision (`ActiveMsgGlobalSettingsModal.tsx`, `apps/VRWorldApp.tsx`) are getting a comment-only follow-up pass — see Progress log. Comments that describe/explain a protected AI-prompt or wire-format regex are still safe to translate (reword the prose) as long as the actual protected pattern content inside the regex/string itself is left untouched — this was already the practice in every batch that did translate comments.

1. **UI TEXT** — labels, titles, placeholders, alt/aria-label, toasts, settings copy. Translate freely once a file's batch comes up. Confirmed the large majority of `components/` (152 files / ~8055 CJK lines) and `apps/*.tsx` (76 files / ~11011 CJK lines) is this category, but **every file in these folders still needs a pass for embedded protocol literals** (see below) — this app renders protocol-adjacent content (card summaries, quoted-message previews, tag-stripped display strings) inline inside otherwise-ordinary UI components, it's not cleanly separated like a naive split would assume.

   **Sub-case, standing rule (user decision, 2026-09-01, mirrors a prior-project precedent): a label that's injected into an AI-facing prompt/context block as informational text — but never parsed/matched back out of AI output — is safe to translate, not protected.** Example: `utils/memoryPalace/types.ts`'s `PLATE_TITLES` (门牌标题) is a single-source `Record<PlateRoom,string>` used both as a plain UI label (`RoomPlatePanel.tsx`, `MemoryPalaceApp.tsx`, dev `console.log`s) **and** as a section header in `formatRoomPlatesSection`'s output, which gets injected into the character's context (function name literally says "注入格式" = injection format). No regex/`.includes()`/`===` anywhere matches against these Chinese strings — the AI never has to echo them back — so translating just changes what section-header wording the AI reads as context, the same category of "flag it, no technical risk" as the prior project's `notifyMascotPageContext({ label })` case (see the standing rule further below). **This is a different risk tier than OBSERVE's `label` field** (which WAS parsed back — genuinely protected) or the bracket/tag protocols (also parsed back). When evaluating a new file: "does this string get matched/parsed back out of LLM output" is the real dividing line, not merely "does this string get sent to an LLM at all."

2. **AI PROMPT / PROTOCOL TEXT** — do **NOT** touch without asking first. Confirmed protocol shapes found so far (this app has *more* distinct tag systems than the prior similar project, not fewer — do not assume prior project's shapes apply here):
   - **Bracket/XML-style wire-format tags taught to the LLM and parsed back**, found in `utils/chatParser.ts` (consumer) paired with `utils/chatPrompts.ts` and friends (producer):
     - `[[MUSIC_ACTION:add|...]]` — music action tag.
     - `[引用:...]` / `\[(?:QU[OA]TE|引用)[：:]...\]` — quote tag, **bilingual**: accepts both `QUOTE`/`QUATE` (English) and `引用` (Chinese) as the tag keyword. Also a "X引用了Y「quoted text」" variant using `「」` corner brackets.
     - `[回复"..."]` — reply tag using Chinese `回复` + curly/corner quote chars.
     - `<语音>...</语音>` and `<字幕>...</字幕>` — **the tag names themselves are Chinese words** (语音 = voice, 字幕 = subtitle), not just Chinese content inside English tags. Translating the words "语音"/"字幕" anywhere near this system risks breaking the regex that matches the literal tag name.
     - `[Transfer]`, `[协同文件：...]`, `[系统: ... 新增了日程 "..."]` — transfer/collab-file/schedule system-notice tags, mixed English/Chinese.
   - **`⟦OBSERVE⟧...⟦/OBSERVE⟧` block protocol** — **RESOLVED (2026-09-01)**: wire-format field names renamed 时间/地点/状态/细节 → `time`/`location`/`status`/`detail` in `utils/datePrompts.ts`'s `OBSERVE_DIMENSIONS`, doc updated, tests updated, build+tests pass. See Progress log for full detail. `docs/date-observe.md`, `components/date/ObserveHUD.tsx`, `components/date/ObserveSettings.tsx` confirmed to have no separate hardcoded copy of the label (single source of truth via `dim.label`).
   - **`<续>`/`<标题>`/`<主题>`/`<起笔>`/`<动态>` tags** (legacy-compat `<续句>`/`<第一句>`) in `utils/vrWorld/prompts.ts`'s `parseSignalOutput` (docs/signal-poetry.md) — another Chinese-tag-name wire format, cross-user "relay poem" feature, backend counterpart in `worker/post-office/src/index.ts`. Not yet individually read/confirmed.
   - **Memory summarization prompts — CONFIRMED (2026-09-01)**: `utils/memoryPalace/pipeline.ts` and `digestion.ts` read in full, both PARTIALLY protected. See Progress log for exact line ranges of the protected subset vs. confirmed-safe UI/dev-log text.
   - **CONFIRMED PROTECTED, entire file (2026-09-01, read in full)**: `utils/songPrompts.ts`, `utils/lifeSimPrompts.ts`, `utils/guidebookPrompts.ts`, `utils/htmlPrompt.ts`, `utils/thinkingChainPrompt.ts`. New tag instance found: `songPrompts.ts:401` rejects LLM output lines starting with literal `json|歌词|示范|建议|解释|原因|说明` + colon (self-contained producer+consumer, same file).
   - `chatPrompts.ts` (659 CJK lines), `datePrompts.ts`'s non-OBSERVE parts (writing-style preset blocks etc.), `promptMessageCleanup.ts`, `applyAssistantPostProcessing.ts` (581 CJK lines — likely output post-processing, i.e. also consumer-side), `sanitize.ts` (274), plus per-feature `utils/groupChat/prompts.ts`, `utils/like520/prompts.ts`, `utils/vrWorld/prompts.ts`, `utils/worldHome/prompts.ts` — **still not yet individually read/confirmed**, treat as protected until read.
   - `utils/personaSimParser.ts` — **CONFIRMED CLEAN (2026-09-01)**: zero Chinese content, no action needed, can be dropped from the watch list.
   - `utils/videoParser.ts` — **CONFIRMED PARTIALLY protected (2026-09-01)**: line 124's `d.type === '图片'` matches a third-party API's (apizero) own response field — different risk category (external API contract), still do-not-touch. `ERROR_MESSAGES` (67-74) and inline `throw new Error(...)` user-facing strings are safe to translate normally.
   - `utils/chatParser.ts` — parser/consumer-side file by name, not yet individually confirmed (distinct from the already-read files above).
   - **NEW, found 2026-09-01 while translating `constants.tsx`'s app names**: `utils/messageFormat.ts`, `features/collaboration/makers.ts` (specifically the fake phone-mockup HTML template at `makers.ts:421`, sent as an LLM design brief), `utils/qixiMemoryBundle.ts`, `utils/qixiReunion.ts` — all embed app-name-like strings directly into LLM-bound message/prompt content. Not yet individually read in full; treat as protected until confirmed. Also `apps/PersonaSim.tsx:1274-1277` — a JSON app-list scaffold embedded in an AI prompt, protected even though the rest of that file isn't necessarily.

   **Rule of thumb (same as prior project, still holds here)**: if a Chinese string is wrapped in a bracket/angle-bracket tag, assigned to a `content:`-like field of a message/session object, passed to a message-adding function, or matched/compared against LLM output or message content — leave it and report it. Static UI labels for the same feature (buttons/menus/settings copy) are usually safe, **except** where the label is proven to double as protocol data (see OBSERVE case above) — check before assuming.

## Scope note (open questions — ask, don't assume)

- **`worker/` (Cloudflare Workers backend, 37 files / ~5168 CJK lines)**: **user decision (2026-09-01): out of scope for this pass**, same as the prior project's `app/api/**/route.ts` — will be a separate pass later. Checked for strings that reach the UI verbatim (error responses shown via toast/alert) before fully setting it aside:
  - `post-office/`, `instant-push/`, `xhs-lite/`, `proactive-push/`, `mcp-proxy/` — **zero** CJK `error`/`message`/`msg`/`reason` field literals found. Nothing here to flag.
  - `amsg/` (the proactive-message background-job worker) is the only subfolder with CJK in error/message-shaped fields — but all found instances read as **tool-call feedback returned to the LLM** (natural-language guidance the AI reads to decide its next action), not literal user-facing UI toasts:
    - `amsg/src/index.ts:1189,1196,1204,1358,1365,1405,1418,1428,1474` — `message:` fields on MCP tool-call results for `schedule_active_message`/cancel/reschedule (e.g. `"对方还没回复，这期间你已经发了/排了 N 条...这次别排了"` = "they haven't replied yet, you've already sent/queued N, don't schedule this time") — addressed to the AI, mirrors `utils/agenticToolFeedback.ts`'s pattern client-side.
    - `amsg/src/plateFire.ts:75,101` — `reason:` fields, same tool-feedback shape (memory "门牌" room-plate organizing job).
    - `amsg/src/selfUpdate.ts:163,173,201,203,208`, `amsg/deno-proxy.ts:166` — operational/diagnostic messages (Worker self-update mechanism, deno proxy health check) — read as ops/log text, not app UI.
    - `amsg/src/index.ts:2026`, `amsg/src/nativeFcm.ts:76,122`, `amsg/src/plateFire.ts:64` — bare `throw new Error(...)` with CJK, all internal diagnostic codes (`AMSG2_KIND_HANDLER_MISSING`, FCM OAuth/payload errors) — read as server-side exceptions, not user-facing.
  - **Not 100% verified these never surface in UI** (didn't trace every fetch call-site in `utils/activeMsgClient.ts`/`activeMsgRuntime.ts` back to a toast) — logged here so it isn't forgotten; re-check call sites before assuming when `worker/` scope is picked up.
- **`utils/` test files** (`*.test.ts`, huge CJK counts — e.g. `activeMsgRuntime.test.ts` 793 lines, `activeMsgClient.test.ts` 429 lines): not UI, likely realistic chat-scenario fixtures in Chinese. Assumed out of scope (mirrors prior project's "not `.tsx`" rule) but flagging since the volume is unusually large — confirm this assumption holds before skipping silently.
- **`docs/*.md` and `plans/*.md`** (upstream dev documentation, mostly Chinese): dev docs, not UI, out of scope for a UI translation pass — but note some of these docs are exactly what should be *read* (not translated) during the protocol-mapping phase, e.g. `docs/date-observe.md`, `docs/signal-poetry.md`, `docs/memory-system-overview.md`, `docs/relationship-system.md`, `docs/mcp-client.md`, `plans/amsg2-*.md`. Read-only reference material for us, not a translation target unless asked.
- **`utils/` non-prompt logic files with heavy CJK** (e.g. `db.ts` 398 lines, `storageOptimize.ts`/`.test.ts`, `realtimeContext.ts`, `apiCallLog.ts`, `analyticsSnapshot.ts`, `context.ts`): not yet individually triaged — could be dev-facing comments (out of scope, like the prior project's rule 1) or could be user-facing error/log strings that reach the UI. Needs a per-file check when its turn comes up, don't blanket-assume either way.
- **This CLAUDE.md file itself and other `.md` docs**: dev documentation, `.md` not `.tsx`, out of scope for the UI pass per the original instruction — not touching unless asked.
- **`trackEvent(...)` analytics calls — RESOLVED, new cross-cutting rule (2026-09-01)**. Discovered via `docs/analytics.md`: this repo's own written rule for adding tracking events (`## 加新埋点的规矩`, rule 1) says event names are **deliberately** written in Chinese so they match what the user sees on screen ("事件名用中文，跟用户在界面上看到的说法一致——按 F12 的人不用再对照一份英文 slug 表"). 710 `trackEvent(...)` call sites found across 77 files in `components/`+`apps/` (roughly a third of all files in scope). Backend is a self-hosted umami instance (`stats.friedsully.com`) that buckets historical data by the exact event-name string.
  - **User decision: translate `trackEvent` event names and property keys/enum-value literals in lockstep with the UI text they describe, accepting that this permanently splits analytics continuity for any renamed event** (old Chinese-named data and new English-named data become separate series in the dashboard — a one-way, non-reversible cost on the data side, distinct from anything in our own repo).
  - **How to apply**: when translating a UI label/button/toast, if the same file has a `trackEvent('<matching Chinese phrase>', ...)` call describing that action, translate the event-name string to match the new English UI wording (not necessarily a literal word-for-word translation — match the *sense*, like the Chinese original does with its own UI). Do the same for property keys (e.g. `错误类型` → `Error Type`, `所在App` → `App`) and for literal Chinese enum *values* assigned to those properties (e.g. `'没配'` → `'Not configured'`) — but leave English enum tokens alone (`'webdav'`, `'github'`, etc. are already provider ids, not translatable). **Do not touch runtime-derived values** passed into `trackEvent` (anything not a literal string in the source — the doc's own rule 4 already forbids passing runtime strings like `error.message`, so anything already runtime-derived was never a literal to begin with).
  - **Consistency requirement**: reuse the same English wording for the same Chinese concept every time it recurs (e.g. every `打开X` "opened app X" event, every `错误类型` property) — add new event-name/property-key translations to the Glossary section below as they're coined, and check the Glossary first before inventing a new rendering. This matters more here than for ordinary UI text because analytics dashboards group by exact string match.
  - **Structural shortcut worth knowing**: the `打开X`-family "opened an app" events (~30 of them, one per installed app) are generated from **one central place** — `context/OSContext.tsx` interpolates the app's display name from the single app list in `constants.tsx`, per `docs/analytics.md`'s "打开某个 App" section. Translating that one app-name list should be verified to automatically retranslate all ~30 of those events, rather than needing 30 separate manual edits — confirm this by reading `context/OSContext.tsx`'s event-firing code before assuming.
  - `docs/analytics.md` itself is out of scope (a `.md` doc, per the bullet above) — not translating its prose, but its "完整事件清单" (full event list, lines ~276-1181 at time of writing) is the authoritative list of every Chinese event name currently in the codebase and is useful as a cross-reference/checklist while translating, even though the doc file itself won't be edited.

## Progress so far

Scouting pass complete (2026-09-01): CJK distribution mapped per top-level directory, protocol file candidates identified by name + spot-checked. User reviewed the risk map and gave scope decisions (see Scope note above); translation phase started.

**`constants.tsx` app-name translation — done (2026-09-01, background agent).** Translated all 28 `INSTALLED_APPS[].name` + `HIDDEN_APP_NAMES` entries to English (full old→new table in this session's agent transcript; highlights: 神经链接→Neural Link, 记忆宫殿→Memory Palace, 小小窝→Dwelling, 家园→Homeland, 彼方→Beyond [matches docs/signal-poetry.md's own "彼方(VRWorld)" gloss], 时光契约→Time Pact, 都市人生→City Life). Confirmed `context/OSContext.tsx:1033`'s `trackEvent(\`打开${appName}\`)` and `components/os/AppErrorBoundary.tsx:59` both read the name dynamically — no separate edit needed, ~30 analytics events auto-translated for free. `pnpm run build` passed, no stray smart quotes/`\uXXXX`.

Two important discoveries from this batch, added to the risk map:
- **NEW: a second, independent "OS skin" label system exists in `components/os/`** (`MobileGameHome.tsx`, `TamagotchiHome.tsx`, `CardbookCompanionChrome.tsx`, `CatCompanionChrome.tsx`, `IdolCompanionChrome.tsx`, `MagazineCompanionChrome.tsx`, `OtomeCompanionChrome.tsx`, `CompanionHome.tsx`) — each hardcodes its **own copy** of app-name-like labels for alternate desktop/launcher visual themes, NOT sourced from `constants.tsx`'s `INSTALLED_APPS`. `constants.tsx` was **not** a single source of truth for app names after all — these skins still show Chinese labels post-rename, a now-more-visible inconsistency. Needs its own dedicated batch when `components/os/` comes up (note `os/CompanionHome.tsx` is already separately flagged as protected/deferred for its analytics-property-key concern, see below).
- **NEW files confirmed protected** (app names embedded in LLM-bound content, found while grepping for app-name cascades): `utils/messageFormat.ts`, `features/collaboration/makers.ts` (a fake phone-mockup HTML template sent as an LLM design brief, `makers.ts:421`), `utils/qixiMemoryBundle.ts`, `utils/qixiReunion.ts` — add to the protected-files list alongside the already-known `chatPrompts.ts`/`memoryPalace/pipeline.ts`/`vrWorld/prompts.ts`. Also `apps/PersonaSim.tsx:1274-1277` has a JSON-scaffold app-list embedded directly in an AI prompt — that specific block is protected even though the rest of the file may not be.
- Deferred (not a problem, just sequencing): ~100+ `trackEvent(...)` calls containing app-name substrings, and several standalone JSX headings matching app names, in files whose own UI-translation turn hasn't come up yet (e.g. `apps/JournalApp.tsx`, `apps/MemoryPalaceApp.tsx`, `apps/DateApp.tsx`, `apps/VRWorldApp.tsx`, `apps/Character.tsx`, `apps/Settings.tsx`, and others) — translating just the event name or just one heading ahead of the rest of that file's Chinese UI would look inconsistent, so left for that file's own batch, per the "translate trackEvent in lockstep with surrounding UI" rule.

**OBSERVE wire-format field rename — done (2026-09-01).** User decision: since no real character/save data exists yet, renamed the OBSERVE block's default wire-format field names from Chinese to English in one commit, no data migration needed:
- `utils/datePrompts.ts`: `OBSERVE_DIMENSIONS[].label` changed 时间→`time`, 地点→`location`, 状态→`status`, 细节→`detail` (single source of truth — this is the exact string injected into the LLM system prompt as the field name, e.g. `time｜（hint...）`). Updated one now-stale dev comment describing "固定中文" labels to reflect the change (comment-only fix, not a translation pass on comments generally). Deliberately did **not** touch `OBSERVE_FIELD_RE`/`mapObserveKey` — the parsing regex already accepted both Chinese and English field-name variants as tolerance for model drift, so it now doubles as backward-compat support for the old Chinese wire format (harmless to keep, costs nothing, matches "no migration needed" instead of a hard cutover).
- `docs/date-observe.md`: updated the wire-format example block's field-name prefixes to match (`时间｜`→`time｜` etc.), added a dated changelog note explaining the rename and that consumer files needed no code change. Rest of the doc (prose, everything else) intentionally left in Chinese — it's dev documentation, out of scope for this pass per Scope note.
- `components/date/ObserveHUD.tsx`, `components/date/ObserveSettings.tsx`, `components/date/DateSession.tsx`: **read in full, confirmed no hardcoded duplicate of the Chinese labels exists** — both HUD and Settings reference the field name dynamically via `dim.label`/`resolveObserveFields(...).display`, so the `datePrompts.ts` change alone was sufficient for wire-format consistency. No code edits needed in these three files for the protocol change itself; their surrounding pure-UI text (theme names, buttons, hint prose) is left as-is for the normal `components/date/` batch later, not folded into this focused commit.
- `utils/datePrompts.test.ts`: 4 assertions were checking the *produced* prompt output against the old Chinese labels (`'状态｜'`, `'时间｜'`, `.toBe('地点')`) — updated to the new English values; one test's Chinese title updated for accuracy (中文→英文 key). Left all other OBSERVE tests untouched — they test the *parser's* tolerance for Chinese-labeled input, which still passes since the fallback regex is unchanged.
- Verified: `pnpm vitest run utils/datePrompts.test.ts` → 28/28 pass. `pnpm run build` → succeeds (`vite build`, 1m17s). `pnpm exec tsc --noEmit` shows pre-existing errors in unrelated files (CompanionHome.tsx, vite.config.ts, etc., not touched by this change) — confirmed via `git status` that only the 4 files above were modified; none of the tsc errors are in them.
- Grepped the 4 touched files for stray smart quotes / `\uXXXX` escapes — clean.

**Deep-read triage of protected files — done (2026-09-01, background agent, Explore subagent_type).** Read in full: `utils/songPrompts.ts`, `utils/lifeSimPrompts.ts`, `utils/guidebookPrompts.ts`, `utils/htmlPrompt.ts`, `utils/thinkingChainPrompt.ts`, `utils/personaSimParser.ts`, `utils/videoParser.ts`, `utils/memoryPalace/pipeline.ts`, `utils/memoryPalace/digestion.ts`. Findings:
- **Fully PROTECTED (entire file, do not translate any of it)**: `songPrompts.ts`, `lifeSimPrompts.ts`, `guidebookPrompts.ts`, `htmlPrompt.ts`, `thinkingChainPrompt.ts`. All are AI system/user-prompt builders; several also have style/label constants that *look* like UI data but are directly interpolated into the prompt (e.g. `songPrompts.ts`'s `SECTION_LABELS[...].label`), so nothing in them is safely separable.
- **New protocol instance found, add to watch list**: `songPrompts.ts:401` — `/^(?:json|歌词|示范|建议|解释|原因|说明)\s*[:：]/i` rejects LLM output lines starting with these literal Chinese words (a lyric-line sanity filter). Producer and consumer are both in this one file.
- **`utils/personaSimParser.ts`** — confirmed **zero Chinese content**, nothing to do here, can be dropped from the watch list entirely.
- **`utils/videoParser.ts`** — PARTIALLY protected: `videoParser.ts:124`'s `d.type === '图片'` compares against a **third-party API's** (apizero) own response field value — a different risk category (external API contract, not our AI/UI text) but equally do-not-touch. `ERROR_MESSAGES` (67-74) and a few inline `throw new Error('...')` calls are ordinary user-facing error text, safe to translate normally.
- **`utils/memoryPalace/pipeline.ts`** — PARTIALLY protected: most of the file (console.log/warn dev diagnostics, `onProgress?.()` UI progress-indicator strings) is safe UI/dev text. Protected subset: `charContext`/diary-ingestion string construction (lines ~1517-1536, ~1743-1757, labels like `[角色档案]`/`【交换日记】`) that gets sent directly as LLM message content, plus a "correction tag" pattern (~line 2007) written for the LLM to re-read on next recall.
- **`utils/memoryPalace/digestion.ts`** — PARTIALLY protected: two full `systemPrompt` blocks (lines 280-358, 998-1022) are protected in full. `DigestReportSection` labels (lines 791-819, e.g. '阁楼困惑'/'窗台期盼') are confirmed UI-only (rendered in the Memory Palace app) and safe to translate — but check against `PLATE_TITLES` in `./types` (not yet read) for consistency first. Borderline: a couple of `tags:` string literals (644, 679) get fed back into a *future* prompt call via `.join(', ')`, recommend leaving them alone even though nothing parses them via regex.
- Full per-file detail (exact line numbers, quoted regex/strings) is preserved in this session's agent transcript — re-derive by re-reading these 9 files if this summary isn't enough detail when their turn comes up.

CJK file/line counts per top-level directory (`.ts`/`.tsx`/`.js`/`.jsx` only, via `grep -rlP`/`grep -rP` with GNU grep `-P` PCRE unicode range `[\x{4e00}-\x{9fff}]` — no ripgrep available in this environment, see Environment notes):

| Dir | Files w/ CJK | CJK lines | Notes |
|---|---|---|---|
| `utils/` | 642 | 36453 | Bulk of protocol/prompt code lives here flat, not in a dedicated folder |
| `apps/` | 76 | 11011 | Top-level screen components, mostly UI but several embed protocol rendering inline |
| `components/` | 152 | 8055 | Mostly UI, per-feature subfolders |
| `worker/` | 37 | 5168 | Backend, scope TBD |
| `context/` | 2 | 824 | Not yet triaged |
| `hooks/` | 2 | 685 | Not yet triaged |
| `features/` | 11 | 429 | Not yet triaged |
| `public/` | 2 | 103 | Not yet triaged |
| `scripts/` | 3 | 76 | Likely dev tooling, out of scope |
| `api/` | 3 | 7 | Not yet triaged |
| `test/` | 1 | 8 | Out of scope (test) |
| `server/` | 1 | 2 | Not yet triaged |
| `netlify/` | 1 | 1 | Not yet triaged |
| root (`.ts`/`.tsx` directly in repo root) | 8 files present (`types.ts`, `constants.tsx`, `App.tsx`, `index.tsx`, etc.) | not yet counted individually | `types.ts` is 179KB — likely has Chinese doc-comments on types, not UI strings |
| `docs/`, `notes/`, `plans/`, `pixelroom/`, `cloudflare/`, `infra/`, `icons/`, `assets/`, `tools/`, `更新日志/` | 0 code files w/ CJK (docs are `.md`, out of scope) | — | — |

`utils/` subfolder breakdown: `memoryPalace/` 84 files/4615 lines, `vrWorld/` 15 files/1174 lines, `worldHome/` 8 files/837 lines, `groupChat/` 17 files/530 lines, `like520/` 1 file/521 lines. The remaining ~28776 CJK lines in `utils/` are in flat top-level `utils/*.ts` files (see file list in "Two categories" above).

**Batch 2 — done (2026-09-01, 3 parallel background agents + manual finish-up).** Translated to English: `components/appearance/` (2 files), `components/journal/JournalAppearanceEditor.tsx` (`JournalThemeArtwork.tsx` had zero UI strings, untouched), `components/user/PerCharAvatarPicker.tsx`, `components/luckin/` (4 files), `components/mcd/McdMiniApp.tsx`, `components/novel/NovelWriter.tsx`, `components/bank/` (all except `BankShopScene.tsx`, still excluded/protected per its narrative-AI-prompt content), `components/events/qixi/QixiBGM.tsx` + `QixiDemoEvent.tsx`.

Notable per-file protocol findings during this batch (beyond what was already known):
- `NovelWriter.tsx` builds a `novel_card` message with `content: "[笔友会小说]..."` (translated to `"[Pen Pal Novel]..."`) — **confirmed safe to translate**: traced through `utils/messageFormat.ts` and `utils/messageFormat.novelCard.test.ts` (which explicitly documents "novel_card 的 content 只是占位...真正的章节归档在 metadata.novel 里" — the `content` field is just a placeholder, the real AI-facing chapter data lives in `msg.metadata.novel`, confirmed also via `MessageItem.tsx` rendering from `metadata.novel` not `content`). Still left protected in the same file: a full inline AI prompt template (storyContext + chapter-summary prompt sent to `chat/completions`), and a character `memories` `summary:` field (long-term memory content).
- `QixiDemoEvent.tsx` (the largest/most complex file in this batch, 1446 lines): confirmed protected — `SCENES[].title` (read by `utils/qixiReunion.ts:307` and `utils/qixiBridge.ts:144`, interpolated into LLM prompts as `sceneName`), and a regex in `wishCardText` construction (`.replace(/^(?:写|许愿)[：:]\s*/, '')`) that strips a literal Chinese prefix off AI-generated option labels from `utils/qixiMemoryBundle.ts` — translating that regex would break stripping on real Chinese AI output. Confirmed **safe and translated**: `sceneChoicePrompt` object (UI hint text shown to the human player, not sent to any LLM — verified by its render call site) and the `window.render_game_to_text` debug-dump's `visibleActions` array (checked it's cosmetic/QA-only, real click targets use stable `data-qixi-action` attributes not the Chinese text).
- `constants.tsx`'s `INSTALLED_APPS`/`HIDDEN_APP_NAMES` app names — see the dedicated entry above (this was actually part of the same working session as Batch 2 but tracked separately since it was its own agent).

**Bugs found and fixed during this batch's verification** (both now fixed, build + full test suite green):
1. **Real syntax error**, not just a translation nit: a mid-edit slip put a straight-apostrophe English contraction ("you'd") inside a single-quote-delimited JS string in `QixiDemoEvent.tsx:1225`, prematurely closing the string (`'Pick another personality word you'd use for them.'`) — `esbuild`/`vite build` failed with `Expected ":" but found "d"`. Fixed by rewording to avoid the contraction (`'...word to describe them.'`). Swept all Batch 2 files for the same contraction-inside-single-quote pattern afterward — no other instances found (everything else with an apostrophe was safely inside JSX text nodes or double-quoted strings, which don't have this problem). **New standing check for future batches**: after translating, grep touched files for `[a-zA-Z]'(t|s|d|ll|re|ve|m)\b` and manually verify each hit isn't inside a single-quoted JS string literal — English contractions are a real, recurring syntax-breaking risk that Chinese source text never had.
2. **"Wiring tests" that assert exact source-text substrings broke** when the underlying UI text was translated — not a functional regression, just stale expected strings. Found via `pnpm vitest run` (full suite, not just the build): `utils/companionHomeRuntimeReferences.test.ts` (expected old `写歌` in `constants.tsx`), `utils/journalAppearance.test.ts` ×2 (expected `预览并浏览`/`日记美化急救：恢复原版` in `JournalAppearanceEditor.tsx`), `utils/qixiEntry.wiring.test.ts` (expected `你挑中`/`另一边偷偷自购`/`从摊位上挑一件你真的想买的商品`/`另一层挤了进来`/`另一层传来` in `QixiDemoEvent.tsx` — 3 of these were "hidden" behind the first failing assertion in the same `it()` block since Vitest stops at the first throw per test, found by manually cross-checking every Chinese literal in that test file against current source). All 4 test files updated to the new English strings, verified against actual source content (not guessed). **New standing rule for future batches**: run `pnpm vitest run` (the *full* suite, not just files colocated with the edit) after every translation batch, not only `pnpm run build` — these wiring tests live in `utils/` and have no naming/location relationship to the `components/`/`apps/` files whose text they pin, so they're easy to miss otherwise. When one fails, also manually check for sibling Chinese-literal assertions later in the same `it()` block that the test runner didn't get to yet.

Also: this batch's original agents were interrupted mid-task by an account-level API session/usage limit (not a bug or a stuck agent) partway through `components/events/qixi/QixiDemoEvent.tsx` and the final build-verification step of `components/luckin/`+`mcd/`+`novel/`; work was picked up and finished directly (not re-delegated) once observed, including the syntax-bug fix and all 4 test-file corrections above.

**`utils/memoryPalace/types.ts`'s `PLATE_TITLES` — done (2026-09-01, manual, single-source translation).** Translated the 4 room-plate titles (TA的事/我是谁/我们之间/我的领域 → Their Story/Who I Am/Between Us/My Domain). **User decision**: confirmed safe to translate even though it's injected into the AI's context (via `formatRoomPlatesSection`/`buildRoomPlatesInjection` in `utils/memoryPalace/roomPlates.ts`) — see the new standing rule added under "Two categories" above (informational-only AI context, never parsed back, same risk tier as the prior project's `notifyMascotPageContext`). All 8 usage sites checked (`apps/MemoryPalaceApp.tsx`, `RoomPlatePanel.tsx`, `digestion.ts`, `roomPlateCore.ts`, `roomPlates.ts` ×4) — all either UI display or dev `console.log`. One test fixed: `utils/memoryPalace/roomPlates.test.ts` asserted the old Chinese title in a `formatRoomPlatesSection` output check — updated to `'Between Us'`/`'My Domain'`. `pnpm vitest run utils/memoryPalace` → 290/290 pass, isolated `pnpm exec vite build` → pass.

**`components/character/RoomPlatePanel.tsx` — done (2026-09-01, manual).** Full UI translation (header comment, hint text, notices, buttons, entry list). Left protected: `TAG_ICONS`' match arrays (`['家庭','家人']` etc.) — literal Chinese substrings matched via `.includes()` against the AI-assigned `tag` field on each plate entry; translating them would silently break icon selection for all future AI-tagged entries.

**Batch 3 — in progress (2026-09-01, 3 parallel background agents).** Dispatched:
- `components/lifeRecord/LifeRecordPanel.tsx` (solo agent, given care due to a real dual-purpose risk found in the sibling `utils/lifeRecords.ts` file) — **done**. Full UI translation including a rewritten date formatter (English month abbreviations instead of Chinese 月/日). Confirmed `THEMES.cn` (tab labels: period/med/expense/exercise) is a **separate, UI-local** dictionary from `utils/lifeRecords.ts`'s own `LIFE_MODULE_LABELS` (same 4 Chinese words, but a different, unrelated, un-imported constant used only for AI-prompt injection in that other file) — safe to translate, confirmed via full read of both files and a repo-wide import check. **Left protected/flagged for follow-up**: `utils/lifeRecords.ts` itself (out of scope, genuine protocol file — builds AI-context summaries and defines a real wire-format tag protocol `[[LIFE:PERIOD_START]]`/`[[LIFE:PERIOD_END]]` etc., first spotted back in the original scouting pass's chatParser.ts comments, now confirmed) — its `medFreqLabel()` function is called from within the now-translated `LifeRecordPanel.tsx` and will keep rendering Chinese frequency text ("每天"/"隔天") inside the otherwise-English panel until `utils/lifeRecords.ts` gets its own dedicated pass. **Naming to reuse later**: this batch rendered "生活记录注入" as "Life Record Injection" (footer copy) — `apps/Character.tsx:1480` has the same toggle label, still untranslated; match this exact wording when that file's turn comes up (added to Glossary).
- `components/character/` (5 remaining files: `CharacterGroupFilter.tsx`, `ChibiStudio.tsx`, `CreatorPartsUploader.tsx`, `ImpressionPanel.tsx`, `MemoryArchivist.tsx`) + `components/schedule/` (all 4 files) — **done**. `pnpm run build` passed (2m2s); full `pnpm vitest run` 4486/4489 passed, the 2 failures both pre-existing/unrelated (flaky `storageOptimize.test.ts`, and `updateNotificationReferences.test.ts` failing against the *other* concurrent agent's still-in-flight `UpdateNotificationEvent.tsx` edit — correctly left alone as out of this agent's scope). No wiring tests needed fixing. Notable findings:
  - `MemoryArchivist.tsx:125`'s `[角色记忆精炼: ...]` label and `:110`'s `mood || '无'` fallback are both injected into an LLM-bound prompt (traced to `apps/Character.tsx:514`'s `handleRefineMonth`) but never parsed back out of AI output — translated per the informational-only-context standing rule (same tier as `notifyMascotPageContext`/`PLATE_TITLES`), now `[Character Memory Refinement: ...]` and `'none'`.
  - `CreatorPartsUploader.tsx`'s displayed recolor-marker instructions (`#色`/`#原色`) translated to `#tint`/`#notint` — confirmed safe since the actual parser (`utils/psdCreatorImport.ts:70-72`, untouched/out of scope) already accepts both Chinese and English spellings via regex.
  - `CreatorPartsUploader.tsx`'s category chip labels (from `utils/creatorCategories.ts`'s `CC_CATEGORIES[].label`, e.g. "肤色"/"眼睛") remain Chinese — that file wasn't in scope for this batch, flagged as a follow-up (same "single source not yet translated" pattern as the `components/os/` alternate-skins finding).
- 13 standalone top-level `components/*.tsx` files (`BackupReminderEvent.tsx`, `BuildBadge.tsx`, `ChatBroadcast.tsx`, `DevDebugPanel.tsx`, `InstantPushSunsetEvent.tsx`, `PhoneShell.tsx`, `QixiLaunchPopup.tsx`, `UpdateNotificationEvent.tsx`, `VRBroadcast.tsx`, `WorkerUpdateReminderEvent.tsx`, `WorldBroadcast.tsx`, `Amsg2DebugPanel.tsx`, `Like520Event.tsx`) — **done**, ~890 CJK lines translated (`Like520Event.tsx` alone was 270 lines / ~3900 total lines, the largest file in this batch — a "520" seasonal event, galgame-style chibi scene + letter/puzzle views). `pnpm run build` passed; full `pnpm vitest run` → **358/358 files, 4489/4489 tests pass** after one test fix.
  - **Protected, found via careful data-flow tracing**: `Like520Event.tsx:3278`'s `userName = userProfile.name || '你'` fallback — flows into a `cardData.userName` that gets `JSON.stringify`'d into a persisted `role: 'assistant'` chat message (`DB.saveMessage`), re-sent to the LLM as history in future turns. Left in Chinese. Two visually-identical fallbacks elsewhere in the same file (lines 3513, 3536) were confirmed to be local-only `LetterView`/`DoneView` display props, never persisted — those were translated to `'you'`. Good example of the same-looking pattern needing per-instance verification, not a blanket file-level judgment.
  - `PhoneShell.tsx:122-227` has an entire dead `AppErrorBoundary` class inside a `/* */` block comment (superseded by a real import) — left untouched like any other comment, doesn't compile/run.
  - Test file fixed: `utils/updateNotificationReferences.test.ts` (2 of 4 assertions were stale Chinese substrings from `UpdateNotificationEvent.tsx`) — updated to match actual translated source, verified not guessed.
  - `trackEvent` event names/properties/enum-values translated in lockstep across all 13 files per the analytics rule (e.g. `弹出版本更新提醒`→`Show Version Update Reminder`, property `范围`→`Scope`).

**`components/ValentineEvent.tsx` + `components/WhiteDayEvent.tsx` — done (2026-09-02).** Both import `injectMemoryPalace` like `QixiDemoEvent.tsx`, but the actual protocol boundary found differs in shape from both Qixi and Like520:
- Fully protected (untouched): inline AI system/user prompt template literals sent via `fetch(.../chat/completions)` (`ValentineEvent.tsx:536-566`, `WhiteDayEvent.tsx:661-717/811-845/923-929/954/1322`); chat-history placeholders embedded in LLM-bound strings (`'[图片]'` image placeholder, `[最近记录...]:` wrapper labels, `'未作答'` not-answered fallback); a persisted `DB.saveMessage` system-notice string (`ValentineEvent.tsx:593`) that re-enters future LLM context, same treatment as Like520's persisted `userName`.
- **New sub-case (2026-09-02): `injectMemoryPalace` query-hint strings treated as protected/flag-worthy even though nothing parses them back** — these are embedding-search queries against the character's (Chinese-language) memory store; translating could degrade semantic recall quality without breaking anything outright. This is a *functional-quality* judgment call, not a safety/parsing one — deliberately NOT auto-applied under the "informational-only, never parsed back" standing rule (that rule is about safety, not quality), left for explicit review later.
- **New instance of the existing "informational-only AI context" standing rule** (same tier as `PLATE_TITLES`/`MemoryArchivist`): `WhiteDayEvent.tsx`'s room-decor `descriptionPrompt`/`itemName`/`itemDesc` fields, traced into `apps/RoomApp.tsx:744`'s room-observation prompt injection — confirmed never parsed back, translated.
- Two canvas-drawn/attribute strings found only via comment-excluding grep on a second pass (`WhiteDayEvent.tsx:1233`'s baked-in postcard text, `:1690`'s `alt="自定义图片"`) — pure UI output, translated.
- `pnpm run build` passed; full `pnpm vitest run` — 356/358 files, 4487/4489 pass, the 2 failures (`utils/amsgInstantChat.wiring.test.ts`, `utils/amsgStateSync.wiring.test.ts`) confirmed unrelated — they assert against `components/settings/ActiveMsgGlobalSettingsModal.tsx`/`apps/Settings.tsx`, both still mid-translation by a separate concurrent agent at the time (see below).
- New glossary: Qixi (七夕, English proper-noun form, matches `QixiDemoEvent.tsx`'s existing styling). Note: "星月梦境童话" also appears as a separate, unrelated, untouched literal in out-of-scope `utils/qixiChatCard.ts:69` — a known duplicate, not a functional risk.

**Batch 4 — `components/call/` (11 files) — done (2026-09-02).** All UI text translated. Left protected: `VRMVideoCallStage.tsx:381`'s regex `/文件已丢失|找不到 model3\.json|模型包内找不到/i`, matching literal Chinese error strings thrown by out-of-scope `utils/live2dModelStore.ts` — translating would break "model missing" detection. `callLightTheme.ts` had zero UI strings (CSS-generation logic, only dev comments). No `trackEvent` calls in this folder. Test file fixed: `utils/callAppRuntimeReferences.test.ts` (12 stale Chinese assertions across 5 test blocks, verified against actual translated source). New glossary terms: Call Preferences, "Who speaks first" / "They speak first" / "I speak first", Speak up after silence, Date portrait, Static shot, Fake camera, Local emotion, Snapshot per turn, Action Library / Action Lab, Stage tools, Anchor face.

**Batch 4 — `components/handbook/` (19 files) — done (2026-09-02).** 14 files edited (~90 strings); 5 files (`FragmentCollage.tsx`, `JournalAnnotations.tsx`, `JournalCanvas.tsx`, `JournalRichText.tsx`, `paper.tsx`) verified via full read to contain zero rendered UI strings (only comments) and correctly left untouched. Left protected: `paper.tsx`'s `dayOfWeekZh()` (Chinese values only ever compared via `===`, never displayed raw — both consumer files were in-scope and handled). **Not Chinese, correctly left alone**: `paper.tsx`'s `seasonLabel().jp` and `HandbookCover.tsx`'s "季節の手帳" are deliberate **Japanese** design flourishes (JP_STACK font), not Chinese — same for `JournalCanvas.tsx`'s `moods` array and part of `stickers.tsx`'s `KAWAII_INTERJECTIONS` array (only that array's Chinese items were translated, Japanese items left alone). Confirmed `utils/handbookGenerator.ts` (imported type-only by `HandbookCharPicker.tsx`) IS a real AI-prompt builder (`fetch`/`chat/completions` at lines 459-464, 701-706) but only its *type* is imported here, no string values flow through — nothing to protect in this component. No `trackEvent` calls. New glossary terms: 小生活→"Little Life", 我的一天→"My Day", 便签/便笺→"Note", 收下→"Keep", 撕掉→"Tear out", 入册/不入册→"Included"/"Excluded" (state) vs "Include it"/"Exclude it" (tooltip action), 今日感恩→"Today's Gratitude", 我→"Me" (author fallback). **Follow-up flag**: `utils/trackerSeeds.ts`'s `TRACKER_TEMPLATES[].name/.blurb` (rendered inside the now-translated `TrackerCreateSheet.tsx`) are still Chinese — same "producer not yet translated" pattern as other findings, needs its own pass.

**Batch 4 — `components/settings/` (9 files) — done (2026-09-02).** All 9 files translated (`ActiveMsgGlobalSettingsModal.tsx`, `AndroidUpdateControl.tsx`, `ApiCallLogModal.tsx`, `InstantPushSettingsModal.tsx`, `McpConnectionConsole.tsx`, `PushSubscriptionPanel.tsx`, `PushVapidSettingsModal.tsx`, `StorageUsagePanel.tsx`, `VersionInfo.tsx`). Left protected: `McpConnectionConsole.tsx`'s `failureKind` regex block (~lines 169-176) matches literal Chinese error text thrown by out-of-scope `utils/mcpFireCore.ts` — translating would silently break connection-failure classification; left an inline comment explaining why.

**Batch 4 — `components/settings/ActiveMsgGlobalSettingsModal.tsx` (1601 lines, the largest settings file) — done (2026-09-02).** 35 `trackEvent` calls across 14 event names translated in lockstep (e.g. 一键部署 2.0 后端→"One-Click Deploy 2.0 Backend", 即时对话能不能开→"Can Instant Chat Be Enabled"), plus ~60+ other UI strings (modal chrome, confirm dialogs, toasts, health-check/deploy/Instant-Chat cards). All Chinese code comments (including a large changelog block) deliberately left untouched, same as every other batch. **New "producer not yet translated" dependency gap found**: `utils/amsgDiagnostics.ts` (out of scope) still returns Chinese literals for `diagnosticRows`/`INSTANT_CHAT_BLOCKER_HINTS`/`resolveInstantChatBlocker()` — these will keep rendering in Chinese inside this now-mostly-English modal, and one `trackEvent` analytics property (`result` on the "Can Instant Chat Be Enabled" event) will carry **mixed English/Chinese values** until that util gets its own pass — flagged, not fixed. New feature-name coinage: **实时感知 → "Real-time Perception"** — check consistency when `apps/Settings.tsx` (not yet translated) reaches its own batch, since it references the same feature. 3 wiring test files fixed (`activeMsgClient.wiring.test.ts`, `amsgStateSync.wiring.test.ts`, `amsgInstantChat.wiring.test.ts`), verified against actual translated source.

**Batch 4 wrap-up**: full `pnpm vitest run` across the entire repo (all of batch 4's files together) → **358/358 test files, 4489/4489 tests pass**. `pnpm run build` passes.

Note on how batch 4 was executed: several of these background agents self-organized by spawning their own sub-agents to split large file lists (e.g. the call/handbook agent split into a dedicated `call/`-only and `handbook/`-only sub-agent; settings did similarly, including a dedicated sub-agent for `ActiveMsgGlobalSettingsModal.tsx` alone) after getting confused mid-task believing other unrelated concurrent agents' file changes (visible via `git status`) were "sibling agents" they needed to coordinate with or wait for — this required a few rounds of explicit correction (clarifying no coordination was needed, listing exact remaining files) before they proceeded. All agents in this batch also independently hit an account-level API session/usage limit partway through (same as batch 2) and were resumed via SendMessage once it reset, rather than restarted from scratch. Final results were still fully verified (build + full test suite) and consistent despite the mid-task confusion and interruptions.

**Batch 5 — `components/os/CompanionHome.tsx` (275 CJK lines, the largest "OS skin" file) — done (2026-09-02).** All 4 `trackEvent` calls translated including **property keys, not just values** (the highest-risk part of this task): `{ 形象, 语音 }` → `{ 'Avatar Type', Voice }`, `{ 用途, 语种 }` → `{ Purpose, Language }`, plus their literal enum values (`静态图片`/`见面立绘`/`动态模型` → `Static Image`/`Date Portrait`/`Animated Model`; `开机`/`触摸` → `Startup`/`Touch`). No LLM/prompt calls found in this file at all — pure UI/settings chrome. Two test files fixed: `utils/companionAnalyticsRuntime.test.ts` (also extended its "no PII in analytics" negative-filter check to cover the new English property key names, so that safety check doesn't silently lose coverage) and `utils/companionHomeRuntimeReferences.test.ts`. **Cross-file consistency confirmed**: `apps/Appearance.tsx` independently duplicates one of these events (`切换桌面见面立绘衣服`) and a concurrent agent translating that file separately landed on the *exact same* English wording (`'Switch Desktop Date Portrait Outfit'`) without coordination — verified via the shared wiring test. New glossary terms: Moon Court (月庭, otome skin home), Night Patrol (夜巡, cat skin home), Feature Constellation (功能星盘).
**Mid-batch bug found**: a concurrent agent's edit to `apps/Appearance.tsx:1277` introduced a curly/smart quote as a JSX attribute delimiter (`className=”...”`) — build-breaking, caught by this agent's own build verification even though it hadn't touched that file. Flagged back to the responsible agent to fix in its own sweep (see below).

**Batch 5 — rest of `components/os/` (24 files) — done (2026-09-02).** `pnpm run build` pass; full `pnpm vitest run` → **358/358 files, 4489/4489 pass**. Files read but correctly left untouched (zero UI strings, comments only): `AppIcon.tsx`, `CdnImg.tsx`, `TokenImg.tsx`, `acnhIcons.tsx`, `mobilegameArt.tsx`.
- `MobileGameHome.tsx`'s `getChibi` import from `utils/vrWorld/chibi.ts` traced and confirmed a pure asset-resolution helper (image/scale/offset), not a protocol file — safe.
- **New pattern found**: `CompanionLockChrome.tsx`/`CardbookCompanionChrome.tsx` used `Intl.DateTimeFormat('zh-CN', ...)` to format an on-screen date — this renders real Chinese text regardless of any hardcoded string nearby. Switched to `'en-US'`. **Add this to the standing checklist**: when translating a file, also grep for `Intl.DateTimeFormat`/`toLocaleDateString`/similar with a `'zh-CN'`/`'zh'` locale argument — that's a second, easy-to-miss source of Chinese output.
- **Real bug found via byte-level check**: an Edit call had silently written the literal 6-character text `—`/`…` into the source instead of an actual em-dash/ellipsis character (confirmed with `od`, not just `grep`). Fixed by using plain ASCII (`-`, `...`) instead. **Reinforces the standing `\uXXXX`-escape sweep rule** — this is exactly the failure mode it's meant to catch, now confirmed to actually occur, not just theoretical.
- Producer-not-yet-translated gaps flagged: `gotchiScheme.ts`'s `SCHEMES[].name` (11 Chinese scheme names, e.g. 星云紫/奶油白) still render inside `MobileGameHome.tsx`/`TamagotchiHome.tsx`'s now-English palette panels. `TamagotchiHome.tsx`'s `FALLBACK_DEFAULT`/`FALLBACK_SULLY` furniture `.name` fields were translated despite being used as a fallback lookup key (`saved[item.id] || saved[item.name]`) — judged low-risk (fallback-only, pre-real-roomConfig) but flagged for awareness.
- Not Chinese, correctly left alone: `MagazineCompanionChrome.tsx:37`'s "きみと過ごす時間の記録" — Japanese design flourish, same precedent as `handbook/`.
- Test file fixed: `utils/companionHomeRuntimeReferences.test.ts` (9 assertions, verified against actual post-translation source; a parallel agent concurrently edited other sections of the same test file for `CompanionHome.tsx` — confirmed no conflict, both edit sets landed cleanly).

**Batch 5 — first pass at `apps/` (17 files, the first batch touching apps/ rather than components/) — done (2026-09-02).** `pnpm run build` passes; full `pnpm vitest run` → **358/358 files, 4489/4489 pass**, no wiring-test fixes needed anywhere in this batch (all pre-existing tests referencing these files already matched or asserted only code-structure, not Chinese UI strings). Split across 2 parallel agents:
- **Larger screens** — `Appearance.tsx`, `ThemeMaker.tsx`, `SocialApp.tsx`, `WorldbookApp.tsx`. `SocialApp.tsx` ("Spark") has 3 full AI-prompt templates (feed refresh/comments/replies) fully protected; confirmed safe to translate its `[分享帖子]`→`[Shared Post]` message placeholder via the same "producer rebuilds from metadata, ignores raw content field" trace used for the earlier NovelWriter `novel_card` precedent. `WorldbookApp.tsx` flagged (not fixed): `WORLDBOOK_POSITION_LABELS`/`WORLDBOOK_ROLE_LABELS`/`WORLDBOOK_POSITION_DESCRIPTIONS` from out-of-scope `utils/worldbook.ts` will keep rendering Chinese inside its now-English dropdowns. ~17 `trackEvent` names + properties translated. **This is where the recurring curly-quote corruption bug (see Environment notes) was caught a 2nd and 3rd time** — in `Appearance.tsx` and again across `ThemeMaker.tsx`/`WorldbookApp.tsx`, fixed both times with scoped `perl -CSD` substitutions.
- **Smaller screens** — `BrowserApp.tsx`, `CharCreatorDevApp.tsx`, `FAQApp.tsx`, `Gallery.tsx`, `HandbookApp.tsx`, `HotNewsApp.tsx`, `Launcher.tsx`, `MusicApp.tsx`, `NovelApp.tsx`, `UserApp.tsx`, `VoiceDesignerApp.tsx`, `XhsFreeRoamApp.tsx`, `XhsStockApp.tsx` — all 13 done, ~500 strings total. `HandbookApp.tsx` handled per its documented caution: `dayOfWeekZh()`-related weekday-character literals left untouched (this screen renders/compares against them directly, per the earlier `handbook/` batch note). **New risk category found**: `BrowserApp.tsx:266-267`'s `url.includes('小红书')`/`url.includes('哔哩哔哩')` match literal Chinese site names against a **user-typed browser address-bar string** — not an AI-protocol match, but the same "leave literal Chinese alone, it's compared against something outside our control" principle applies; translating would break site detection for anyone who types the Chinese site name. `MusicApp.tsx:527`'s `keyword: '晴天'` is a real search term sent to the actual NetEase Music API in a diagnostic self-test — left as real functional input, only the surrounding button label was translated. `VoiceDesignerApp.tsx`'s `PREVIEW_SAMPLES` (zh/yue/ja demo text sent to MiniMax TTS to preview pronunciation) left untouched — same principle as the earlier `voice-settings.tsx`-style precedent (translating would make a "preview this language" action speak the wrong language); matches the array's own existing convention of leaving ko/fr/es labels in native script too. `MusicApp.tsx:154`'s `title="未来音楽"` is deliberate Japanese-kanji branding (未来音楽 uses 楽, not simplified 乐) — left alone, matching the established Japanese-design-flourish precedent.

**Batch 6 — `components/date/` (4 files: DateSession.tsx, DateSettings.tsx, ObserveHUD.tsx, ObserveSettings.tsx) — done (2026-09-02).** `pnpm run build` passes; full `pnpm vitest run` → **358/358 files, 4489/4489 pass**. No AI-prompt/protocol text found in any of these 4 files (they only reference the already-renamed OBSERVE wire format from an earlier batch, structurally untouched). One `trackEvent` fixed: `设置见面语音语种`→`'Set Date Voice Language'` with property key 语种→Language. Only remaining CJK anomaly: `ObserveHUD.tsx`'s `ink` theme had a literal Chinese `headerLabel: '观 · 录'` (the brief had assumed all `headerLabel` values were already English like the other 4 themes' `'OBSERVE'`/`'OBSERVE://READOUT'`) — translated to `'OBSERVE · RECORD'` for consistency; purely cosmetic, never parsed. Also fixed one build-breaking curly-quote corruption (the recurring bug, see Environment notes) introduced mid-edit in `DateSession.tsx`'s Exit/Message-Options modals.

**Batch 6 — `components/date/story/` (8 files: StoryMaskBox, StoryPresetMaker, StoryQuickPresetPanel, StoryTheater, StoryTheaterEditor, StoryTheaterSession, StoryTheaterTheme, StoryVectorMemoryPanel) — done (2026-09-02).** `pnpm run build` passes; full `pnpm vitest run` → **358/358 files, 4489/4489 pass**. `utils/storyTheater*.ts` (the producer/backend for this feature) confirmed to contain zero protocol markers — pure UI batch except for one real find:
- **New protected protocol layer found in `StoryTheaterSession.tsx`**: its `StoryOutput` renderer parses AI-generated story text via a colon-delimited `label：value` line format (`row.match(/^([^：]{1,14})：(.*)$/)`) and matches specific Chinese literals against the parsed labels. Left protected: `AFFINITY_DIMENSIONS` array (信任/安全感/占有拉力/情绪压强/修复意愿), the temperature-label match arrays (角色对你的温度/关系温度/你对角色的温度/你的关系温度), `groupDisplayLines` anchor/ignore-label arguments (主体/幕后暗格/起因/镜头债/人物/角色 ID), `line.label ===` comparisons (心声/真正的独白/场面/时间/地点/推进 — this last one is a *different* protected use of "推进" than the safe transcript-builder use below), the `/^\S+小剧场[｜·]?\s*/` name-prefix-stripping regex (word "小剧场" is match-critical), and `mask.name.trim() !== '你'` (depends on the literal default in out-of-scope `utils/storyTheater.ts:290`).
- **Confirmed safe and translated per the tier-2 rule**: the inline compression system-prompt (`把剧场片段压缩成一只可长期常驻上下文的事件盒...`), the transcript-builder's `'推进'`/`'正文'` role-label prefixes (a separate, safe use from the protected `line.label === '推进'` comparison above — this one is prose fed *to* the AI, never parsed back), and the `剧情：${entry.title}` user-message template.
- Repo-wide grep for '推进'/'正文'/'剧情' found one unrelated real parse-back — `apps/theater/TheaterPanel.tsx:91` matches "正文" against AI output in a completely separate "theater" feature with no code path connecting to StoryTheaterSession.tsx. No collision risk, but worth knowing before that file's own batch.
- Two `//` comments were initially missed by the translating agent (lines ~661-662, ~686-687 — plain logic-explanation comments, not protocol-pattern descriptions) and were translated in the follow-up verification pass.

**Batch 6 — `components/chat/` (22 files, the highest-risk remaining folder) — done (2026-09-02), split across 3 parallel agents.** `pnpm run build` passes; full `pnpm vitest run` → **358/358 files, 4489/4489 pass**.
- **`MessageItem.tsx` (458 CJK lines, solo/careful agent)** — the message-rendering + protocol-stripping component. All protected regex/tag patterns verified intact and unmodified: `翻译`/`原文`/`译文` bilingual-tag regex, `聊天|通话|约会` source tags, `QU[OA]TE`/`引用` quote tags, the imitated-history `引用了...「」` pattern, `回复"..."` reply tag, `ACTION|RECALL|SEARCH|DIARY|READ_DIARY|FS_DIARY|FS_READ_DIARY|SEND_EMOJI|DIARY_START|DIARY_END|FS_DIARY_START|FS_DIARY_END` tags, `schedule_message` tag, `<语音>`/`<字幕>` voice/subtitle tags, `<#seconds#>` TTS pause markup, `VALID_INTERJECTION_TAGS`, `%%TRANS%%`/`%%BILINGUAL%%` markers. **Two new bracket-tag protocols found and added to the protected list**: `/^\[(System|系统|System Log|系统记录)\s*[:：]?\s*/i` (matches the already-documented `[系统: ...]` tag) and `/^\[小屋动态\]\s*/` ("Dwelling life update" system prefix). Also protected: `displayText.includes('任务'|'纪念日'|'转账')` icon-selection matching (same risk category as the `BrowserApp.tsx` site-name precedent) and a `/[「」""\"]/` quote-detection heuristic matching AI-generated dialogue. The `THINKING_CHAIN_PRESETS` bilingual `titleZh`/`titleEn`/`listenLabel`/`silenceLabel`/`quoteLeft`/`quoteRight` fields left as-is across **all 11 presets** (not just the `'心象'`/`'PSYCHE'` one — all 11 share the same bilingual-card design pattern), plus the ink-seal decoration's literal `心` character (line 344, a decorative glyph, not text). Fixed several hardcoded `'zh-CN'` locale calls → `'en-US'` (the standing checklist item). Fixed 2 stale test files (`utils/messageItemModuleLayout.test.ts`, `utils/collaborationWiring.test.ts`).
- **Settings/modal cluster (`ActiveMsg2SettingsModal.tsx`, `ChatModals.tsx`, `ChatInputArea.tsx`, `ChatFineTunePanel.tsx`, `ThinkingChainSettingsModal.tsx`)** — fully translated. One protected `<语音>` tag reference left in a `ChatModals.tsx` TS-interface comment. This agent independently discovered and confirmed the "Psyche" terminology (see Glossary correction above) before the MessageItem.tsx agent finished, and both converged on the same answer.
- **Remaining 17 smaller files** (CharacterEntryTransition, ChatHeader, ChatHeaderShell, ChromeCssEditor, EmotionSettingsPanel, HtmlCard, InstantChatRouteNotice, LuckinCard, LuckinCheckoutCard, McdCard, MemoryRepairPortal, ProactiveSettingsModal, QixiEventCard, ScheduleChangeNotice, VoiceFavoritesPortal, WhiteboxSoundEditor, ChatConstants.ts) — 16 of 17 fully translated (`trackEvent` lockstep applied, e.g. `把瑞幸购物车发给角色`→`'Send Luckin Cart to Character'`). Fixed a hardcoded `'zh-CN'` locale call in `VoiceFavoritesPortal.tsx`.
  - **`ChatConstants.ts` — RESOLVED 2026-09-02 (follow-up pass)**: `DEFAULT_REFINE_PROMPTS`/`DEFAULT_ARCHIVE_PROMPTS`' `.content` fields (real AI prompt templates, monthly-memory-refinement and daily-archive-summarization prompts sent to the LLM) are now fully translated, including the `关键词: ...`→`Keywords: ...` marker. Confirmed safe by tracing both consumers: `apps/Character.tsx`'s `handleRefineMonth` (`summary = extractContent(data)`, saved verbatim to `refinedMemories`) and `apps/Chat.tsx`'s daily-archive flow (`summary = extractContent(data); summary.replace(/^["']|["']$/g, '')`, saved verbatim to `MemoryFragment.summary`) — neither parses or splits on the `关键词:`/`Keywords:` marker anywhere; it's purely a human-readability instruction to the AI, never read back by code. No stored legacy data to worry about (fresh dev repo, same as the earlier OBSERVE precedent). Repo-wide grep for "关键词" found dozens of unrelated hits (all a different, generic use of the word "keyword(s)" for search/tags/worldbook features) — none reference this specific trailing-marker format. Build + full test suite verified green after the change.
  - `LuckinCard.tsx:219`'s `takeCode !== '生成中'` literal confirmed protected — matches the sibling `components/luckin/LuckinMiniApp.tsx` (already translated, same literal left in Chinese there too) — compared against a value read from the simulated Luckin API's own response field.
- **Recurring account-level session-limit interruptions**: all 5 original agents in this batch (plus 2 self-spawned sub-agents for the date/ and date/story/ splits) hit the same account session-limit wall at least once, some twice, across two different reset windows (~4:40pm and ~10:30pm Asia/Jakarta on 2026-09-02) — all resumed via SendMessage (never restarted) once each window passed, preserving full progress/context each time. One agent (date/ coordinator) initially treated the resume message with justified generic suspicion (a message arriving via task-notification channel, no way to cryptographically verify origin) but correctly proceeded by independently verifying repo state itself rather than trusting the claim — this is the right posture and worth preserving as a norm for future multi-agent coordination in this project.

**Batch 7 — first wave of remaining `apps/*.tsx` (low-risk cluster, per the batch-7 scouting report) — done (2026-09-03).** Scouted first via a dedicated research-only agent across all 22 remaining `apps/*.tsx` files before touching anything (see the scouting summary folded into this batch's notes below). This wave covered the 7 files the scouting report rated LOW risk: `DateApp.tsx`, `QQBridge.tsx`, `StudyApp.tsx`, `WorldHomeApp.tsx` (one agent), `VRWorldApp.tsx`, `MemoryPalaceApp.tsx`, `Settings.tsx` (one agent each, given their size — 778/788/949 CJK lines respectively). `pnpm run build` passes; full `pnpm vitest run` → **358/358 files, 4489/4489 pass**.
- **`DateApp.tsx`** (190 CJK lines, 21 trackEvent) — fully translated; prompt-building confirmed centralized in out-of-scope `utils/datePrompts.ts` (file's own comment says so), no inline AI prompts here. Protected: `current.includes('完成')` in `runMemoryPalacePostHook` (line 416) — a substring match against status text emitted by out-of-scope `utils/memoryPalace/pipeline.ts`, which still returns Chinese stage strings; translating would silently break the success-toast detection. Left a code comment explaining why.
- **`QQBridge.tsx`** (44 CJK lines, 1 trackEvent) — fully translated, lowest-risk file in the whole project so far. No protocol content, no protected fetch calls.
- **`StudyApp.tsx`** (130 CJK lines, 15 trackEvent) — fully translated. The translating agent made a judgment call worth recording: two Chinese fragments embedded inside otherwise-English AI-prompt-building code (a `typeLabels` dict for quiz-type descriptions, and the `resultsText` mark/label builder in `submitQuiz`) were translated rather than left protected, reasoning that they're purely descriptive labels injected into an English prompt, never parsed back from the AI's response, and leaving them Chinese risked the AI mirroring Chinese words into its English-generated review/quiz content (which *is* shown to the user). A reasonable call, consistent with the tier-2 "informational, never parsed back" rule — just flagging that it required judgment beyond the original scouting brief.
- **`WorldHomeApp.tsx`** (300 CJK lines, 14 trackEvent) — fully translated except one deliberately protected line (325): `shareToChat()`'s `DB.saveMessage` call embeds a `【家园 · X】...发了条动态：` bracket-tag prefix directly into persisted, LLM-visible assistant chat content (it re-enters history on future turns) — left untranslated, flagged with a code comment. Fixed the one `toLocaleString('zh-CN', ...)` locale bug. **New producer-not-yet-translated gap found**: `NARRATIVE_STYLES` (imported from out-of-scope `utils/worldHome/prompts.ts`) still has Chinese `.name`/`.guide` fields, so the now-English "Narrative Style" picker in WorldEditor will keep showing Chinese names/descriptions until that file gets its own pass (added to "Still open" section).
- **`VRWorldApp.tsx`** (778 CJK lines) — UI text/labels/trackEvent/comments now all fully translated (see the comment-backlog follow-up note below — the first pass left all comments in Chinese, fixed in a same-day follow-up per the new standing comment-translation rule). Protected and left untouched: `userProfile?.name || '我'` fallback and the `activity`/`content` template literals in `onUserBoardPost`/`onUserVRBroadcast` — both build real LLM-facing chat content (confirmed via `utils/chatPrompts.ts:1290` reading `msg.content` verbatim as chat history). The `IDLE_QUIPS` chibi ambient-status array (local to this file, indexed purely by position modulo array length, never matched by content or sent to the AI) was confirmed safe and translated, with array shape/order preserved exactly so positional indexing isn't affected. All 4 `zh-CN` locale bugs fixed. 2 stale wiring tests fixed (`utils/vrWorldPdfImport.test.ts`, `utils/featureWiring.guestbookPhoneApi.test.ts`).
- **`MemoryPalaceApp.tsx`** (788 CJK lines, 14 trackEvent) — fully translated. The single `chat/completions` call is a rerank-API dev/QA connectivity self-test (not a real prompt), translated along with everything else. No `role:'system'` prompts, no bracket-tag protocols found anywhere in the file. Fixed **12** `zh-CN` locale bugs (11 pre-identified by scouting + 1 more independently found in a `fmtRangeTs` helper).
- **`Settings.tsx`** (949 CJK lines, 83 trackEvent — the single largest file and largest trackEvent lockstep job in the whole project so far) — fully translated. **Important near-miss, caught and self-corrected**: the translating agent initially translated the instructional text telling users how to activate the McDonald's/Luckin MCP integrations ("send 'X请求' to activate"), which would have told users to type a phrase the app no longer actually recognizes — `麦请求`/`瑞一杯` are literal trigger phrases matched verbatim against user-typed chat text via `MCD_ACTIVATE_TRIGGER`/`LUCKIN_ACTIVATE_TRIGGER` in out-of-scope `utils/mcdToolBridge.ts`/`utils/luckinToolBridge.ts`. Caught via a targeted grep and reverted to the literal Chinese phrase with an explanatory parenthetical — this is exactly the kind of "translating the surrounding instruction breaks a literal match embedded in prose, not in a bracket tag" risk that's easy to miss; worth remembering as its own sub-pattern distinct from the usual bracket-tag protocol risk. Also correctly left protected: several regex literals matching error text thrown by out-of-scope producer files (`/超时/`, `/鉴权失败/`, `/请求失败/` from `utils/mcpClient.ts`; `/无效的文件格式/` etc. from the import-system; `/认证失败/`, `/无法创建/`, `/服务器返回/` from `utils/webdavClient.ts`; `/^恢复失败/` from `cloudRestoreFromWebDAV`). Fixed the `journalAppearance.test.ts` stale assertion (`重置交换日记美化` → `Reset Exchange Diary Styling`, consistent with the "Exchange Diary" reconciliation decision above). All 4 `zh-CN` locale bugs fixed.
- **Comment-translation standing rule established mid-batch (2026-09-03)**: see the new note right after "## Two categories of Chinese text" above. `VRWorldApp.tsx`'s first pass and the earlier `ActiveMsgGlobalSettingsModal.tsx` batch had both explicitly left all Chinese code comments untouched ("per established project convention" at the time) — this was inconsistent with most other batches (which did translate comments) and was flagged to the user, who decided: translate comments everywhere, for future-maintainer consistency. Both files got (or are getting) a same-day comment-only follow-up pass.
- **Scouting summary** (from the dedicated research-only agent dispatched before this batch, covering all 22 remaining `apps/*.tsx` files): risk-tiered the files into HIGH (`CallApp.tsx`, `GameApp.tsx`, `DreamTheater.tsx`, `PersonaSim.tsx`, `JournalApp.tsx`, `ScheduleApp.tsx`, `Character.tsx`, `Chat.tsx`), MEDIUM (`CheckPhone.tsx`, `GroupChat.tsx`, `RoomApp.tsx`, `BankApp.tsx`, `SongwritingApp.tsx`, `GuidebookApp.tsx`, `LifeSimApp.tsx`), and LOW (the 7 files this batch covered). **`CallApp.tsx` flagged as the single highest-risk file remaining**: it has a live in-file consumer regex (`.replace(/^\s*(?:\[\s*通话\s*\]\s*)+/gim,'')` etc., lines 227-228) stripping the literal Chinese words `通话`/`聊天`/`约会` from AI output, PLUS the teaching-instruction prose for the same tag system in the same file (lines 482-483, 487) — both producer-adjacent and consumer-side risk co-located in one file, unlike every other tag-protocol finding so far which splits across files. Recommend translating it last, as its own dedicated careful pass. `Chat.tsx:624` has a literal functional AI directive (`'把以下内容翻译成中文...'` — a real "translate into Chinese" instruction for voice-message translation) flagged for explicit sign-off before translating the wording. Full per-file detail (bracket-tag findings, `zh-CN` locale line numbers, trackEvent counts) is preserved in the scouting agent's own report, not reproduced here in full — re-run or reference that report when each HIGH/MEDIUM file's turn comes up rather than re-scouting from scratch.

**`apps/CheckPhone.tsx` (4071 lines, 748 CJK lines, MEDIUM-risk tier) — done (2026-09-04).** Full UI text, comments, aria/title attrs, and `trackEvent` payloads translated to English, including the `checkPhoneApi` prompt builders inside `handleGenerate` (~lines 756-820, the layout-hint/real-vs-fictional-contact/`perspectiveLock` prompt) and the `handleGenerateAiAgent`/`handleAiSend`/`handleAiAutoContinue`/`handlePlayCard` prompt blocks for the "Agent" sub-feature (assistant/Claude/tavern personas) — all confirmed tier-2 safe (informational prompt content, never parsed back) by tracing `extractContent`/`extractJson` on the response side, which never match against any of this prompt's literal Chinese phrasing.
- **New protected pattern found, not in the prior documented list**: the file's entire relationship/agent dialogue format uses `"我:"`/`"对方:"` as a literal speaker-prefix wire format, actively parsed by `utils/relationshipChat.ts`'s `parseTranscript()` (`/^(我|对方|Me|Them)\s*[:：]\s*(.*)$/`, confirmed bilingual) and re-serialized by `serializeTurns()` (hardcoded to always write back Chinese `我`/`对方`, regardless of input language). Since `utils/relationshipChat.ts` is out-of-scope for this batch and `serializeTurns` cannot be changed without a matching pass there, every literal `我:`/`对方:` occurrence in `CheckPhone.tsx` — inside prompt instructions teaching the AI the format, in local prefix-stripping regexes (`handleAiSend`/`handleAiAutoContinue`), and in the `myPrefix`/`replyPrefix` constants — was left untouched; only the surrounding English prose was translated. Flagging this as a new addition to the "wire-format token, not a bracket tag" family (same category as `<语音>`/`<字幕>`, but plain words instead of angle brackets) for future files that import `parseTranscript`/`serializeTurns` or otherwise construct `我:`/`对方:` scripts (e.g. `apps/GroupChat.tsx`, `apps/Chat.tsx`, `apps/CallApp.tsx` — not yet confirmed, check before assuming they don't share this pattern).
- Confirmed safe and translated (verified via `matchVendor`'s regexes in `getVendorTheme`, all already bilingual e.g. `/豆包|doubao/`, `/gpt|openai|chatgpt|查特/`): the AI-vendor display labels/flavor text (`AI_VENDOR_LORE`, `getVendorTheme`'s `label` fields) — translating them to English (Doubao, ERNIE Bot, Qwen, Kimi, DeepSeek, ChatGPT, Claude) doesn't break vendor-theme matching since the regexes accept English vendor names already.
- Confirmed safe and translated (per the scouting brief + independent verification): the two `phone_card` content-string patterns `[你手机的聊天软件]`/`[你手机的${logPrefix}]` → `[Your phone's Chat App]`/`[Your phone's ${logPrefix}]`, and `[人际关系变动]` → `[Relationship Change]`. Traced `utils/chatPrompts.ts:1317-1322` (reads `msg.content` verbatim as LLM-facing context prose, no parsing) and `components/chat/MessageItem.tsx:2592+` (renders from `metadata.phoneCard` fields, not from `content`, and only matches `pc.kind` against already-English enum values like `'relationship'`/`'chat'`/`ai_${service}` — never against the Chinese `app`/`content` text) — repo-wide grep confirmed no other file references these bracket prefixes.
- 5 `zh-CN`/locale bugs fixed (all `new Date(...).toLocaleString/toLocaleTimeString('zh-CN', ...)` → `'en-US'`), plus a bilingual-safety addition: `renderEvidenceDetail`'s order-progress-step detection (`r.detail?.includes('签收')`) got an added `|| r.detail?.includes('delivered') || r.detail?.includes('Delivered')` check, since the delivery/shopping prompts now instruct the AI to write English detail text.
- 21 `trackEvent` calls translated in lockstep (e.g. `切换查手机独立 API`→`Switch Check Phone Independent API`, `偷看 AI 助手使用记录`→`Peek at AI Assistant Usage History`), including property keys/enum values already using English tokens (`contactKind`, `mode`, `subApp`, etc. — these were already code-level ids, not translated further).
- New glossary terms (see Glossary section below): Check Phone's own app-name UI labels — Chat App (was 聊天软件), Contacts (联系人), Taobao (kept as real brand, was 淘宝), Food (外卖), Moments (朋友圈), Call Log (通话记录), Agent (智能体, the "AI-services-within-a-phone" sub-feature), Deep Talk/Confidant (树洞's tab name+flavor), Tavern (酒馆).
- 1 stale wiring test fixed: `utils/featureWiring.guestbookPhoneApi.test.ts` (`aria-label="查手机 API 设置"` → `aria-label="Check Phone API settings"`, verified against actual translated source, not guessed). Verified `utils/relationshipChat.test.ts` (14 tests, exercises `parseTranscript`/`serializeTurns`/`flipTranscript`) still passes untouched, confirming the preserved `我:`/`对方:` tokens didn't break anything.
- `pnpm run build` passes (`CheckPhone-*.js` chunk built cleanly). Full `pnpm vitest run` → 357/358 files, 4488/4489 pass; the one remaining failure (`utils/amsgStateSync.gaps.test.ts`'s GroupChat anchor-string assertion) is confirmed unrelated — it reads `apps/GroupChat.tsx` (322 insertions/310 deletions uncommitted at the time, a different concurrent session's in-progress translation of that file, per `git diff --stat`), reproduced identically with this session's `CheckPhone.tsx`/test changes fully stashed out.

**`apps/GroupChat.tsx` (343 CJK lines) — done (2026-09-04).** UI text, all `//`/`/* */` comments, aria/title attrs, and `trackEvent` payloads (e.g. `复制一条群消息文字`→`Copy Group Message Text`, `打开群设置面板`→`Open Group Settings Panel`, `切换群聊回复生成模式`→`Switch Group Chat Reply Generation Mode`) translated to English. Confirmed-safe placeholder patterns translated: `[群聊公共话题盒：...]`/`type:'group_topic_card'` (rebuilt from `meta.groupTopicBox` by `utils/chatPrompts.ts:87`) and `[领取红包]`/`[退回红包]`/`type:'transfer'` (rebuilt from `metadata.packetReceipt`/`meta.packet` by `utils/groupChat/format.ts`/`redpacket.ts`). **Left protected as one unified block** (translating only part of a single AI-facing prompt would mix languages mid-prompt): the `` `【系统：群聊模拟器配置】` `` header template, the `getTimeGapHint`/`getPrivateTimeGap` functions, and the `` `<<< 角色档案 START/END >>>` `` character-profile context-injection block (self-referential `[私聊空窗期]` label included) — same class of out-of-scope-adjacent prompt content as `GameApp.tsx`'s `buildSyncContext`. **New protocol found, not previously documented**: a `[html]...[/html]` wire-format tag description inside `triggerDirector`'s HTML-mode prompt extension (`` `【群聊 HTML 适配】[html]...[/html] 块要写在...` ``) — left protected/untranslated pending confirmation of where this tag is actually parsed (not yet traced to a consumer; flag for whoever next touches `apps/Chat.tsx` or `utils/groupChat/dispatch.ts`, since HTML-card rendering likely lives there). One `toLocaleDateString('zh-CN')` locale bug fixed. Found and fixed another instance of the recurring curly-quote corruption bug (in the Group Chat Memory Rules / HTML Prompt help modals) during editing — same standing mitigation as always. A stale test anchor was fixed as part of this batch's consolidated verification (see below).

**`apps/RoomApp.tsx` (2537 lines, 385 CJK lines) — done (2026-09-04).** Full UI text, all comments, `trackEvent` (16 calls, e.g. `生成今天的房间`→`"Generate Today's Room"`, `观察房间里的家具`→`'Observe Furniture in Room'`) translated. The `[系统: ...]` persisted messages (lines ~858/865) and room-observation bracket string (~916/922) translated per the already-documented bilingual-safe `[系统: ...]` family. **`initializeRoomState`'s large structured AI prompt (~lines 780-810) was verified safe and translated in full**: traced the response-handling path (`extractJson(content)`, keyed on already-English JSON fields `actorStatus`/`welcomeMessage`/`items`/`todoList`/`notebookEntry`) — nothing regex-matches or string-compares against the Chinese prompt content or its response, tier-2 safe. One real `toLocaleString()`→`toLocaleString('en-US')` locale bug fixed (a pre-existing locale-less call was correctly left as-is). New glossary terms: 生活碎片→"Life Fragments", 样板房→"Room Template" (matches existing code identifiers `BuiltInRoomTemplate`/`buildRoomTemplate`).

**`apps/SongwritingApp.tsx` (3649 lines, 285 CJK lines) — done (2026-09-04).** Full UI text, comments, and all 16 `trackEvent` calls translated. The 3 previously-flagged unreviewed sites (~lines 491, 868, 935) turned out to be plumbing lines (`{ role: 'system', content: systemPrompt }`); the actual nearby instructional content (`typeHints` dict, `request`/`retryInstruction` strings) was verified tier-2 safe and translated. The `[系统: ...]` template at line ~972 translated per the established family. **Left protected**: an `injectMemoryPalace` query-hint template literal (line ~848, an embedding-search query against the character's Chinese memory store — quality-risk-only per the `ValentineEvent.tsx` precedent, not a parsing-break risk) and a MiniMax style-textarea Chinese example placeholder (~lines 3147/3159, mirrors the `VoiceDesignerApp.tsx` `PREVIEW_SAMPLES` precedent — MiniMax is a Chinese-language TTS model, the adjacent guidance text itself tells users natural Chinese input gets the best results). Self-caught and corrected a mistake mid-translation: a real ¥0.1-0.3/song price was momentarily mistranslated into a fabricated USD conversion, reverted before finalizing. New glossary terms: "Co-writer" (unifying 搭档/创作伙伴, the same AI collaborator role), "Songs We Wrote Together" (一起写的歌, shared album name), "Lyric Notebook" (歌词手帖), "Dual Cover" (合影封面).

**`apps/BankApp.tsx` (89 CJK lines) / `apps/GuidebookApp.tsx` (112 CJK lines) / `apps/LifeSimApp.tsx` (82 CJK lines) — done (2026-09-04).** All three fully translated (UI, comments, trackEvent). `BankApp.tsx`: the mostly-already-English main prompt's small embedded Chinese fragments (记账App/蚂蚁庄园/乐子人) translated as informational tier-2 content; the `[系统: ...]` visit-narration tag translated per the established family. `GuidebookApp.tsx`: confirmed `utils/guidebookPrompts.ts` (fully protected) is only imported/called, no inline prompt content in this file itself; the `score_card` save site confirmed tier-2 safe/irrelevant (`chatPrompts.ts` reduces it to a generic out-of-scope placeholder). Self-caught and fixed one HTML-entity-encoding bug mid-edit (`trackEvent('...&amp;amp;...')` → `&amp;`, would have broken the literal event-name text). `LifeSimApp.tsx`: `createLifeSimResetCardData`'s `score_card` confirmed tier-2 safe (same mechanism as Guidebook); `userProfile?.name || '用户'` fallback translated ('用户'→'User').

**Batch-wide verification note**: this whole medium-risk cluster (CheckPhone.tsx, GroupChat.tsx, RoomApp.tsx, SongwritingApp.tsx, BankApp.tsx, GuidebookApp.tsx, LifeSimApp.tsx) hit the same stale-wiring-test pattern seen before: `utils/amsgStateSync.gaps.test.ts` sliced `apps/GroupChat.tsx`'s source between two comment-text anchors (`'// 轮询模式'` and `'// 触发入口'`) that had been translated away by the comment-translation pass — fixed by updating the anchors to the actual translated comment text (`'// Round-robin mode:'` / `'// Trigger entry point:'`), verified against source. `pnpm run build` passes; full `pnpm vitest run` → **358/358 files, 4489/4489 pass**.

**`apps/ScheduleApp.tsx` (56 CJK lines, 4 trackEvent, 2 `chat/completions` calls) / `apps/JournalApp.tsx` (196 CJK lines, 14 trackEvent, 2 `chat/completions` calls) — done (2026-09-04), from the HIGH-risk `apps/` cluster.** Both fully translated (UI, comments, trackEvent lockstep).
- **`ScheduleApp.tsx`**: the two inline `role:'user'` prompts (task-completion reaction at `generateTaskReward`, anniversary-reminder at `generateAnniversaryThought`, each paired with a `role:'system'` `baseContext`) confirmed tier-2 safe — both produce a single free-text sentence shown as a toast/bubble, never parsed back (response only goes through a quote-stripping regex and gets displayed/persisted as prose). Translated in full. The `` `[系统: ${userProfile.name} 完成了任务 "${title}"。${supervisor.name} 评价道: "${text}"]` `` persisted message translated to `[System: ... completed the task ... commented: ...]` per the established bilingual-safe `[系统: ...]`/`[System: ...]` family. Soft theme's `label`/`eventLabel` (心愿单/纪念日) translated to "Wish List"/"Anniversaries" (cyber/minimal themes were already English, untouched). Checked for the `[html]...[/html]` tag and the `"我:"/"对方:"` wire-format token from `CheckPhone.tsx`'s batch — **neither pattern found in this file**.
- **`JournalApp.tsx`**: `generateProseSummary`'s prompt (archive-summary path, `` `### [系统指令: 交换日记归档]` `` header) confirmed tier-2 safe and translated — repo-wide grep for "系统指令" found only unrelated prose usages of the phrase inside out-of-scope `utils/qixiMemoryBundle.ts`/`utils/storyTheater.ts` (generic mentions of "system instruction" as a concept, not a matched wire-format tag), and the summary this prompt produces is assigned directly to a `MemoryFragment.summary` string field, never parsed. The 2nd `chat/completions` call (`handleExchange`'s "Exchange Diary Mode Instructions" prompt) was inspected: its response is parsed via `extractJson` into fields `text`/`paperStyle`/`stickers`, but those are already-English JSON keys (not matched against any of the prompt's Chinese prose), so the surrounding instructional Chinese was safe to translate — confirmed via `extractJson`/`salvageDiaryText`'s fallback logic, neither of which matches on literal Chinese content. `PAPER_STYLES[].name` (白纸/网格/点阵/横线/夜空/少女 → Plain/Grid/Dot/Lined/Night Sky/Blossom) confirmed cosmetic-only (rendered as a tooltip and inside a `diary_card`'s `userPaperName`/`charPaperName`, which `MessageItem.tsx` only displays and `chatPrompts.ts`'s `diary_card` branch never reads) — translated. Rewrote the diary-list date display from a Chinese `月` suffix to a new `MONTH_ABBR` array (JAN-DEC), matching the `LifeRecordPanel.tsx` precedent of replacing Chinese month/day formatting with English abbreviations. Reused the existing "Exchange Diary" glossary term throughout (not "Diary Exchange"). Checked for `[html]...[/html]` and `"我:"/"对方:"` — **neither pattern found in this file** (now 3 of 3 checked files in this family confirm CheckPhone.tsx is still the only file using the `我:`/`对方:` token).
- One recurring curly-quote corruption (see Environment notes) caught and fixed in a `JournalApp.tsx` code comment (straight `"Save."` → curly `"Save."` during a multi-line Edit) — harmless (inside a comment, not a delimiter) but fixed per the standing sweep.
- One stale wiring test fixed: `utils/journalRerollRuntime.test.ts` (5 assertions checking exact source substrings from `handleExchange`/`saveEntry`/the rewrite button's `aria-label`) — updated to the actual translated strings, verified against source, not guessed.
- `pnpm run build` passes. Full `pnpm vitest run` → 356/358 files, 4485/4489 pass. Remaining failures confirmed unrelated: `storageOptimize.test.ts` (3 tests, known-flaky per its own doc note, passes standalone in isolation — 84/84) and `amsgStateSync.wiring.test.ts` (2 tests, asserts Chinese strings against `apps/Character.tsx`, which `git status`/`git diff --stat` confirmed is a different concurrent session's in-flight translation, 771 lines changed at time of check — same "leave it, not my scope" call as the earlier `UpdateNotificationEvent.tsx`/`GroupChat.tsx` precedents).

**`apps/DreamTheater.tsx` (1175 lines, 252 CJK lines, 10 trackEvent) / `apps/PersonaSim.tsx` (1284 lines, 238 CJK lines, 5 trackEvent) — done (2026-09-04), the two remaining HIGH-risk files each built around one large fully-inline director prompt.** Both fully translated: UI text, all `//`/`/* */` comments, aria labels, toasts, and `trackEvent` names in lockstep.
- **`DreamTheater.tsx`'s `buildDreamPrompt`** (~80 lines, the "Dream Theater director" prompt) confirmed tier-2 safe by reading `generateDreamScript`'s consumer path in full: `parseDream` only strips ```` ```json ```` fences, slices between the first `{`/last `}`, and calls `JSON.parse` (with a control-character repair fallback) — no regex/string match anywhere against the prompt's Chinese content or the model's response. Translated the whole function, including its illustrative example sentences (e.g. "门在微笑。" → "The door is smiling.") since the brief judged changing example tone/style as a non-issue once parsing safety was confirmed. `THEMES[].label` (13 archetype names, e.g. 甜梦→"Sweet Dream") also translated — same tier-2 safety, since `label` only feeds UI display and the forced-archetype prompt override, never anything parsed back; `archetypeNo`'s dev-only hidden-slot marker `'隐'`→`'Hidden'`. `DREAM_BOX_SERIES.title`→"Little Dreams · Meow Dream Blind Box". 4 `zh-CN`→`en-US` locale fixes (one `toLocaleString` call). New protocol layer found: none — no bracket/tag wire-format of any kind in this file, confirmed via full read.
- **`PersonaSim.tsx`'s `buildDirectorPrompt`** (~75 lines, the "Screenlife director" prompt, same shape as `buildDreamPrompt`) confirmed tier-2 safe by reading `utils/personaSimParser.ts` in full (already flagged CONFIRMED CLEAN in an earlier pass, re-verified here): `normalizeBeat` only checks `kind` against an already-English `SIM_BEAT_KINDS` set, every other field passes through untouched — no match against Chinese content anywhere. **App-name placeholder judgment call** (the embedded JSON schema's example `app.name` values, e.g. 微信/百度/闹钟/相册/浏览器/天气/备忘录): romanized real-world brand names to their actual English names — 微信→WeChat, 百度→Baidu — matching the established Luckin/McDonald's/Xiaohongshu precedent of keeping real brands as brands, not inventing substitutes; the *generic* app-type placeholders (闹钟/相册/浏览器/天气/备忘录/音乐/搜索) were translated to their plain English generic names (Alarm/Photos/Browser/Weather/Notes/Music/Search) rather than treated as brands, since they don't name a specific product. This is the same block flagged at the old `PersonaSim.tsx:1274-1277` line numbers in this doc's "Progress so far" section — confirmed identical, now at ~1277-1284 after the surrounding translation shifted line numbers. `describeAcquaintance`/`buildVariation`/`buildPresenceRule`/`buildToneRule` (helper functions whose output is interpolated straight into `buildDirectorPrompt`) translated as part of the same prompt for the same tier-2 reason. **`buildMemoryText` + the `"【一段亲身经历...】"` wrapper in `sendAsMemory`/`LifeLog.sendLog`** (persisted as `sim_card` message `content`, confirmed via `utils/chatPrompts.ts:1311-1315` that `m.content` is read verbatim and injected into the LLM's chat history) were also translated — judged safe by direct precedent with `phone_card`'s content strings (`chatPrompts.ts:1317-1322`, the *very next case* in the same switch, already confirmed-safe-and-translated in the `CheckPhone.tsx` batch under the identical "verbatim-read persisted prose, never parsed back" reasoning) rather than the stricter `ValentineEvent.tsx`/`Like520Event.tsx` persisted-content precedent (which was about single fallback tokens flowing into structured/JSON-shaped fields, a different risk shape). Checked buildMemoryText's `我:`/`对方:` labels (from `l.me ? '我:' : '对方:'`) against the `utils/relationshipChat.ts` wire-format family flagged in `CheckPhone.tsx`'s batch — confirmed via grep this file imports neither `parseTranscript` nor `serializeTurns` and nothing else reads `memoryText` except this file itself, so translated to "Me:"/"Them:" (now 4 of 4 checked files in this family confirm `CheckPhone.tsx` is still the only file using the real wire-format token). New protocol layer found: none — no bracket/tag wire-format elsewhere in this file, confirmed via full read.
- **Pronoun consistency fix**: both files' source used a Latin-script "ta"/"TA" placeholder embedded directly in Chinese prose as a gender-neutral third-person pronoun. Initially rendered as literal lowercase "ta" in `DreamTheater.tsx` (17 instances), then caught against the standing Glossary rule ("TA → they/them, consistently wherever TA appears as a pronoun, not he/she/it," established in the `components/character/` batch) and corrected to "they/them/their" throughout both files for consistency with the rest of the project (verified via grep that already-translated files like `RoomPlatePanel.tsx` do use "they/them," not literal "ta").
- 1 stale test found, correctly left alone: `utils/amsgStateSync.wiring.test.ts` fails on an unrelated assertion slicing `apps/Chat.tsx` between a `setModalType` anchor and `trackEvent('清空聊天记录')` — `git diff --stat` confirms `apps/Chat.tsx` (658 insertions/520 deletions) is a different concurrent session's in-flight translation with that exact event name already translated away; same "not my scope" call as the `Character.tsx`/`GroupChat.tsx` precedents above.
- `pnpm run build` passes. Full `pnpm vitest run` → 357/358 files, 4488/4489 pass (the one failure being the `Chat.tsx`-caused one above; `storageOptimize.test.ts` did not flake this run). No wiring tests needed fixing for either `DreamTheater.tsx` or `PersonaSim.tsx` specifically (`utils/dateBackgroundBlobRef.test.ts`, `utils/personaSimParser.test.ts` both still pass unmodified).
- New glossary terms (see Glossary section below): Little Dreams · Meow Dream Blind Box (小小梦境 · 喵梦盲盒), Life Log (生活记录, PersonaSim's sub-app — distinct from the unrelated "Life Fragments"/"Life Record" terms already in the Glossary from other features), Persona Simulation sub-labels (Daily/Event Simulation, Your Presence, Performance Tone: Freeform/Melancholic/Dark Humor/Light & Cute).

**`apps/GameApp.tsx` (332 CJK lines, 13 trackEvent) — done (2026-09-04), HIGH-risk.** UI text, comments, toasts, and all 13 `trackEvent` calls translated. Both AI-prompt builders verified tier-2 safe and translated: `buildSyncContext` (the "Neural Link" GM context, traced through `fetchGameAPI` → `extractJson`/`extractContent`, keyed on already-English fields) and `handleGenerateWorld`'s TRPG-worldbuilding prompt (its response IS parsed, by `parseWorldGen` in the same file — but that parser is **already bilingual by design**, `/(?:标题|title)/` / `/(?:世界观设定|世界观|正文|lore)/`, so the prompt's instruction text was translated to ask for the English form while the parser's Chinese regex alternatives were left as backward-compat tolerance, same treatment as the OBSERVE wire-format rename). Confirmed-safe placeholders translated: `[TRPG Progress Card: ...]`, `[TRPG Archive Reminder: ...]`, `[TRPG Game Excerpt] "..."` (`trpg_card`, rebuilt from `metadata.trpg` by `utils/messageFormat.ts`). **Functional (not just wording) change, flagged for visibility**: `handleArchiveAndQuit`'s summarization prompt originally instructed the AI to write its memory summary "in Chinese" — since this text is persisted into character memory/chat display and the rest of the app is now English, the instruction was changed to "in English," matching the precedent this doc already sets for `Chat.tsx:624`'s similar language directive (see below). `pnpm run build` passes; full `pnpm vitest run` → 356/358 files, 4484/4489 pass (2 pre-existing unrelated failures: known-flaky `storageOptimize.test.ts`, and a concurrent session's in-flight `JournalApp.tsx` edit). No new glossary terms (reused "TRPG"/"Neural Link").

**`apps/Character.tsx` (357 CJK lines, 18 trackEvent) — done (2026-09-04), HIGH-risk, the character creation/editing screen.** Full UI text, comments, and all 18 `trackEvent` calls translated. `handleRefineMonth`'s monthly-memory-refinement prompt (header `[角色身份]`→`[Character Identity]`) confirmed tier-2 safe (repo-wide grep: the exact tag isn't matched elsewhere, other `角色身份` hits are unrelated prose) and translated, matching the precedent already set for `ChatConstants.ts`'s `DEFAULT_REFINE_PROMPTS`. **New AI-prompt function found, not previously documented**: `handleGenerateImpression` (~lines 864-1016) builds a full inline JSON-based "impression" generation prompt — traced its consumer (`normalizeUserImpression` in `utils/impression.ts`) and confirmed it only validates already-English JSON keys, nothing matches Chinese prose content, translated under the same tier-2 reasoning as `handleRefineMonth`. "Life Record Injection" toggle (line ~1501) used exactly as reserved. **Correction to an earlier assumption**: 心象/"Psyche" does **not** appear anywhere in this file (`grep` returned zero hits) — the earlier note flagging `apps/Character.tsx` as a file that would need the "Psyche" glossary term when translated was based on the initial scouting pass and turned out to be wrong; no action was needed here. The `"我:"`/`"对方:"` wire-format token also does not appear here (consistent with every other file checked so far — only `CheckPhone.tsx` uses it). One stale wiring test fixed (`utils/amsgStateSync.wiring.test.ts:105`, `'仍然删除'`→`'Delete Anyway'`). `pnpm run build` passes; full `pnpm vitest run` → 4488/4489 pass, 357/358 files (1 pre-existing unrelated failure from a concurrent session's in-flight `Chat.tsx` edit). New terminology: "Design Voice" (捏声音 button), "System Voices"/"Cloned Voices"/"Generated Voices" (系统/复刻/文生音色), "Classic Parameters"/"New Natural Parameters" (MiniMax param versions), "Plates"/"Figures" tab labels (consistent with existing "room-plate"/"Figure Studio" terms).

**`apps/Chat.tsx` (4772 lines, 656 CJK lines, 22 trackEvent) — done (2026-09-04), the largest and most central file in the whole project (per FEATURES.md, tightly coupled with `MessageItem.tsx`/`ChatModals.tsx`).** Full UI text, comments, toasts, and all 22 `trackEvent` calls translated.
- **`llmTranslate` investigation (line ~624), the flagged judgment call — RESOLVED, no functional change made**: the function itself is generic; it has 2 call sites. Line ~670 already dynamically targets `${langLabel}` (the character's configured voice language). Line ~647 is a **separate, always-hardcoded-to-Chinese fallback path** — used only when a `<语音>`-tagged foreign-language message has no `<字幕>` subtitle and no sibling-bubble subtitle can be collected, producing a Chinese "original text" subtitle-equivalent for display; `targetLang` is never referenced there. Only the wording was translated (`'Translate the following into Chinese. Only output the translation, no explanation.'`), preserving the exact current behavior — a foreign-voice character's fallback subtitle will still render in Chinese inside the now-English UI, which is a **pre-existing product characteristic, not a bug introduced or fixed by this translation pass**.
- **`[html]...[/html]` tag — RESOLVES the open question from the `GroupChat.tsx` batch.** Producer: `utils/htmlPrompt.ts` (already fully protected/confirmed). Consumer for 1:1 chat: `utils/applyAssistantPostProcessing.ts` (`5. [html]...[/html] → html_card`). Consumer for group chat: `utils/groupChat/dispatch.ts`. Both out of scope; neither literal string appears in `Chat.tsx` itself (which is why grepping that file alone never surfaced it) — remove this from "Still open," it's now fully traced.
- **New protected pattern found**: `utils/chatPrompts.ts:1179` does `m.replyTo.name === '我'` to pick reply-quote wording taught to the AI, fed directly by `apps/Chat.tsx`'s `replyTarget.role === 'user' ? '我' : char.name` (two call sites: `handleSendText`, `handleQuickReply`, and a third mirrored spot at `senderName` ~line 3060) — left untouched with explanatory comments at each site.
- Confirmed-safe placeholders translated: transfer (`[已收款]`/`[已退回]`/`[转账]`→`[Payment Received]`/`[Payment Returned]`/`[Transfer]`), poke (`[戳一戳]`→`[Poked]`, with a flagged inconsistency: an AI-initiated poke via out-of-scope `utils/chatParser.ts:210`/`[[ACTION:POKE]]` still uses the old Chinese string until that file's own pass), forward (`[转发了...]`→`[Forwarded...]`), McDonald's `mcd_card` content (metadata-rebuilt). **New asymmetry found**: Luckin's `luckin_card` type has NO metadata-rebuild case in `chatPrompts.ts` (unlike `mcd_card`) — its content goes to the AI verbatim, but nothing regex-matches it, so translating was still safe; flagged for a future producer-consistency pass on that file.
- Literal trigger phrases `"麦请求"`/`"瑞一杯"` (`MCD_ACTIVATE_TRIGGER`/`LUCKIN_ACTIVATE_TRIGGER`) kept in Chinese with explanatory comments, matching the `Settings.tsx` precedent. Language presets `'日本語'`/`'中文'` (4 occurrences) left untouched — must match out-of-scope `utils/translationLang.ts`'s `TRANSLATION_LANG_PRESETS` array of native-script language names.
- **Producer gap flagged**: `utils/memoryPalace/types.ts`'s `ROOM_LABELS`/`getRoomLabel()` still returns Chinese room names — the memory-digest result modal's local fallback dict in `Chat.tsx` was translated to the established English room names (Living Room/Bedroom/Study/User Room/Self Room/Attic/Windowsill) for UI consistency, but `getRoomLabel()`'s own output will still be Chinese until that file gets its own pass.
- One stale wiring test fixed (`utils/amsgStateSync.wiring.test.ts`, a `trackEvent('清空聊天记录')` anchor). `pnpm run build` passes; full `pnpm vitest run` → **358/358 files, 4489/4489 pass** (this was the final file in the batch, so this run reflects the whole cluster being consistent together).
- New glossary terms, from the cognitive-digestion result modal: Confusion Resolved, Trauma Deepened, Knowledge Internalized, Self-Insight, New Self-Confusion, User Understanding Synthesized, Worries from Looking Back, New Anticipation, Settled into a Room Plate, Anticipation Fulfilled, Anticipation Unmet, Faded (reused "Anticipation" from the existing 期盼 glossary entry). "Psyche" reused correctly for 心象 (only appears via `.sully-psyche-*` CSS class references, comments, and "Psyche card" modal labels — no prose occurrences needing a fresh decision).

**Batch-wide note (whole HIGH-risk `apps/` cluster minus `CallApp.tsx`)**: `ScheduleApp.tsx`, `JournalApp.tsx`, `DreamTheater.tsx`, `PersonaSim.tsx`, `GameApp.tsx`, `Character.tsx`, `Chat.tsx` — all done. Consolidated verification (`pnpm run build` + full `pnpm vitest run`) after all 7 landed together: **358/358 files, 4489/4489 tests pass**, zero remaining stray CJK outside confirmed-protected content. Only `apps/CallApp.tsx` remains from the original HIGH-risk tier, saved for its own dedicated careful pass as planned (it's the single file where the tag-protocol producer instructions and consumer regex are co-located).

**`apps/CallApp.tsx` (4058→4111 lines, 463 CJK lines, 34 trackEvent) — done (2026-09-04), the single highest-risk file in the whole project (producer teaching-instructions and consumer stripping-regex both live in this one file, unlike every other tag finding which splits across files).** Full UI text, comments, toasts, aria/title/alt/placeholder attributes, and `trackEvent` translated. The `通话`/`聊天`/`约会` source-tag family verified fully intact: both the consumer regex (`.replace(/^\s*(?:\[\s*通话\s*\]\s*)+/gim, '')` etc.) and the producer teaching prose (`buildCallPrompt`, "source tag: [聊天] is what you two typed... do not output system markers like [通话], [聊天], [约会]...") kept every bracketed tag token literal while translating all surrounding sentences — a complete line-by-line accounting is preserved in this batch's own agent report (9 remaining lines, each individually justified). Also found and protected the file's own local producer+consumer for the `<语音>...</语音>` voice tag (same tag family already documented from `MessageItem.tsx`), including its `[语語]` bilingual character-class regex. **Judgment call flagged**: `buildCallPrompt`'s voice-language block instructs the AI to write its primary display line "in Chinese" before the `<语音>` translation — this functional directive was preserved as-is (translated the sentence, kept "in Chinese" as the literal target), following the `Chat.tsx` `llmTranslate` precedent (preserve existing behavior) rather than the `GameApp.tsx` precedent (which changed a similar directive) — worth a human product decision on which precedent should govern this class of case going forward. `toggleStt`'s `startStt('zh-CN', ...)` intentionally left untouched — confirmed via `utils/speechToText.ts`'s own comment that this is the speech-**recognition** input language (a real product setting), not a display-locale bug; the 4 genuine `toLocaleString`/`toLocaleTimeString` locale-format bugs were fixed to `'en-US'`. `"我:"`/`"对方:"` wire-format token confirmed absent (still `CheckPhone.tsx`-only). One 心象→"Psyche" occurrence translated correctly. 2 stale wiring tests fixed (`utils/callAppRuntimeReferences.test.ts`, `utils/companionAnalyticsRuntime.test.ts` — the latter's PII-safety filter also extended to cover new property keys, matching the `CompanionHome.tsx` batch precedent). `pnpm run build` passes; full `pnpm vitest run` → **358/358 files, 4489/4489 pass**.

**This completes the original `apps/*.tsx` scouting scope in full** (LOW + MEDIUM + HIGH risk tiers, all files from the batch-7 scouting report). Every app screen in `INSTALLED_APPS` (see FEATURES.md) is now translated to English.

**`utils/context.ts`'s `ContextBuilder.buildCoreContext` (lines 107-326) — done (2026-09-04), the first pass into the shared foundation used by virtually every non-Story-Theater mode.** This is not one of the `apps/*.tsx` files — it's the single function 15+ apps/components call directly (`BankApp`, `CallApp`, `Character.tsx`, `CheckPhone`, `DreamTheater`, `GameApp`, `GroupChat`, `JournalApp`, `PersonaSim`, `RoomApp`, `ScheduleApp`, `SocialApp`, `SongwritingApp`, `StudyApp`, plus `ValentineEvent`/`WhiteDayEvent`/`BankShopScene`/`StoryTheaterSession.tsx`), and that `apps/Chat.tsx`'s own pipeline (`useChatAI.ts` → `utils/chatRequestPayload.ts` → `ChatPrompts.buildSystemPromptParts`) calls as its own first step — so this single function's translation reaches nearly every mode in the app except Story Theater (which has its own fully separate prompt-assembly system, confirmed not touched by this). Full UI-facing section headers, field labels, and body prose translated (Identity, Self Insights, World Settings, User Profile, Private Impression, Memory Bank, Anti-Filler, plus the JSDoc parameter comments). **Dropped now-redundant parenthetical glosses** per user decision: originally every header was "Chinese (English gloss)" — once the Chinese half was translated, the English half became a literal duplicate (e.g. would have been "World Settings (World Settings)") — these were simplified to just the English term alone (`### World Settings`, `### Memory Bank`, `**Key Memories**`, `**Active Recall**`), while headers where the parenthetical was *already* English before translation (e.g. "Your Identity (Character)", inherited from the original source) were left as-is since those weren't newly-redundant. **New addition, at the user's request**: a placeholder "Global Instructions" section (comment `// 2c. Global Instructions`), inserted after the worldbook `afterExamples` block and before the "User Profile" section — currently always empty (`const globalInstructions = ''`, gated by `if (globalInstructions.trim())`, zero behavior change today) but reserved as the single wire-up point for an app-wide instruction (output language / baseline prose style / etc.) that should apply to every character across every mode that calls `buildCoreContext`, as opposed to the existing per-character `systemPrompt`/`worldview`. No such global-settings source exists yet — this is scaffolding only, flagged with a `TODO` comment. The protected `/[\/年月]/g`/`'日'` date-separator regex (matches literal Chinese date-format characters in stored memory data, not UI prose) was correctly left untouched. Fixed 2 stale test assertions (`utils/datePrompts.test.ts`, `utils/contextVolatileSplit.test.ts`) checking for the old `'表达底线'`/`'### 记忆系统 (Memory Bank)'` strings — confirmed `utils/apiCallLog.test.ts`'s similar-looking hits are self-contained fixture data (constructs its own sample string and checks the parser echoes it back), not assertions against this function's real output, so those didn't need touching. `pnpm run build` passes; full `pnpm vitest run` → **358/358 files, 4489/4489 pass**. **Not yet done**: `buildRoleSettingsContext` (lines 28-96, a separate function in the same file, used for lightweight emotion-evaluation context) is still fully Chinese — out of scope for this pass since the user's instruction specified lines 107-326 only.

## Glossary

Check here before coining a new rendering — reuse existing terms for consistency (also applies to `trackEvent` event-name/property translation, see the analytics rule above).

**App names** (from `constants.tsx`'s `INSTALLED_APPS`, 2026-09-01, also used for the auto-derived `打开X` analytics events): 神经链接→Neural Link, 记忆宫殿→Memory Palace, 电话→Call, 群聊→Group Chat, 小小窝→Dwelling, 家园→Homeland, 查手机→Check Phone, 浏览器→Browser, 见面→Date, 档案→Profile, 存钱罐→Piggy Bank, 交换日记→Exchange Diary, 手账→Handbook, 自习室→Study Room, 笔友会→Pen Pal Society, 写歌→Songwriting, 彼方→Beyond, 时光契约→Time Pact, 世界书→Worldbook, 热点→Hot News, 使用帮助→Help, 相册→Gallery, 自由活动→Free Roam, 小红书图库→Xiaohongshu Gallery, 气泡工坊→Bubble Workshop, 外观→Appearance, 攻略本→Guidebook, 都市人生→City Life, 特别时光→Special Moments, 捏脸·开发→Character Creator (Dev), QQ 桥→QQ Bridge. Note: `components/os/`'s alternate "OS skin" launcher themes have their **own separate**, not-yet-translated copies of similar labels — don't assume these app-name mappings already apply there, see Progress log.

**Other UI/feature terms** (from `components/appearance/`+`journal/`+`user/`, 2026-09-01): 白框/白框自定义→Whitebox/Whitebox Customization (chat CSS override feature), 聊天装扮→Chat Styling, 分角色聊天头像→Per-character chat avatar, 沉浸剧场→Immersive Theater, 紧凑密聊→Compact Chat, 像素终端→Pixel Terminal (chat-shell preset names). **Reconciled 2026-09-02**: 交换日记 → **"Exchange Diary"** (final decision — not "Diary Exchange"). "Exchange Diary" was already the dominant usage (6 occurrences across `JournalApp.tsx`, `MessageItem.tsx`, `JournalAppearanceEditor.tsx`) vs. only 1 outlier ("Diary Exchange" in `constants.tsx`'s app-launcher name), so the outlier was fixed to match. Use "Exchange Diary" everywhere this term appears going forward.

**Batch 2 terms** (`components/luckin/`+`mcd/`+`novel/`+`bank/`+`events/qixi/`, 2026-09-01): 瑞幸/Luckin and 麦当劳/McDonald's — kept as their real-world brand names (Luckin, McDonald's), not translated further. 笔友会小说 (novel_card message-content tag, confirmed safe to translate, see Progress log) → "Pen Pal Novel". 记账 inside `components/bank/` (a bank-app feature, e.g. CSV export filename "记账记录") → "Transaction Records"/"bookkeeping" as plain feature description — **do not confuse with** `lifeRecord/LifeRecordPanel.tsx`'s own separate 记账 (LEDGER) life-record module (now also translated, see below) — same Chinese word, two unrelated features.

**Batch 3 terms** (`components/lifeRecord/`+`character/RoomPlatePanel.tsx`+`memoryPalace/types.ts`, 2026-09-01): PLATE_TITLES: TA的事→Their Story, 我是谁→Who I Am, 我们之间→Between Us, 我的领域→My Domain. LifeRecordPanel's `THEMES[module]` has two label fields per module — `en` was already a fixed short English tag (CYCLE/PHARMACY/LEDGER/TRAINING, unchanged) and `cn` was the actual primary tab-label text shown to the user, now translated: 生理期→Period, 药盒→Pharmacy, 记账→Ledger, 锻炼→Exercise. 生活记录注入 → **"Life Record Injection"** — reuse this exact wording when `apps/Character.tsx:1480`'s matching toggle label is translated (its own batch hasn't happened yet).

**Batch 3 terms, cont'd** (`components/character/`+`schedule/`, 2026-09-01): 捏/捏人器 → "craft"/"Character Creator", 手办柜 → "Figure Studio", 窥视 → "peek", TA → "they/them" (this app's gender-neutral third-person pronoun for the AI character — use "they/them" consistently wherever TA appears as a pronoun, not "he/she/it"), 小剧场 → "theater" (matches the `TheaterPlayer` component name).

**Batch 3 terms, cont'd** (standalone top-level `components/*.tsx`, 2026-09-01): 糯米机 → "Mochi Machine", 手抓糯米机 → "Hand-Grabbed Mochi Machine" (both appear in `BackupReminderEvent.tsx`'s and `PhoneShell.tsx`'s disclaimer copy — reuse if either term appears again elsewhere), 主动消息 2.0 · 即时对话 → "Proactive Message 2.0 · Instant Chat".

**Batch 4 terms** (`components/call/`, 2026-09-02): Call Preferences, "Who speaks first" / "They speak first" / "I speak first", Speak up after silence, Date portrait, Static shot, Fake camera, Local emotion, Snapshot per turn, Action Library / Action Lab, Stage tools, Anchor face.

**Batch 4 terms** (`components/handbook/`, 2026-09-02): 小生活→"Little Life", 我的一天→"My Day", 便签/便笺→"Note", 收下→"Keep", 撕掉→"Tear out", 入册/不入册→"Included"/"Excluded" (state label) vs "Include it"/"Exclude it" (tooltip action wording — same concept, different phrasing depending on context, both correct), 今日感恩→"Today's Gratitude", 我→"Me" (author-name fallback).

**Batch 4 terms** (`ActiveMsgGlobalSettingsModal.tsx`, 2026-09-02): 实时感知→"Real-time Perception" (**new feature-name coinage, check consistency** when `apps/Settings.tsx` — not yet translated — reaches its own batch, it references the same feature). Event names: 探测 2.0 Worker 能力→"Probe 2.0 Worker Capabilities", 即时对话能不能开→"Can Instant Chat Be Enabled", 开启通知与推送订阅→"Enable Notifications &amp; Push Subscription", 一键部署 2.0 后端→"One-Click Deploy 2.0 Backend", 连接并启用主动消息 2.0→"Connect &amp; Enable Proactive Message 2.0", 更新后端 Worker→"Update Backend Worker", 补装后端更新能力→"Attach Backend Update Capability", 切换即时对话→"Toggle Instant Chat".

**Batch 5 terms** (`components/os/CompanionHome.tsx`, 2026-09-02): trackEvent property keys 形象→"Avatar Type", 语音→Voice, 用途→Purpose, 语种→Language; enum values 静态图片→"Static Image", 见面立绘→"Date Portrait", 动态模型→"Animated Model", 开机→Startup, 触摸→Touch. Feature/UI names: 月庭 (otome-skin home screen) → "Moon Court", 夜巡 (cat-skin home screen) → "Night Patrol", 功能星盘→"Feature Constellation".

**Batch 5 terms** (rest of `components/os/`, 2026-09-02): 晴庭 (proper noun) → "Sunlit Court", 花笺 (in-world currency) → "Petal Notes", 星愿收藏册/星愿卡册 → "Star Wish Collection"/"Star Wish Cardbook", 梦幻粉紫 (scheme name) → "Dreamy Pink-Purple", 小窝色 (custom scheme name) → "Room Hue", 归巢 (cat-skin home button) → "Nest", 时光 (short label for Special Moments across 3 skins) → "Moments", 篇章→Chapter, 动态→Feed, 心声 (CheckPhone quick-entry label) → Voice. trackEvent properties: 错误类型→"Error Type", 所在App→App (reuses the CLAUDE.md example mapping), 恢复方式→"Recovery Method", 复制结果→"Copy Result", 等待档位→"Wait Bucket", 开场版本→"Boot Version" (values 完整版/极短版→Full/Minimal), 数据是否已就绪→"Data Ready" (是/否→Yes/No).

**Batch 5 terms** (`apps/` first pass, 2026-09-02): 马甲 (Spark alt-persona feature) → "Alt Account", 触感陪伴 → "Touch Companion", 电子宠物·小小窝 → "Tamagotchi · Dwelling" (reuses existing Dwelling=小小窝 mapping).

**⚠️ Major term — CORRECTED 2026-09-02, batch 6**: **心象 → "Psyche"** (not "Mindscape" — see below). Other new terms from the `apps/MusicApp.tsx` batch (2026-09-02): 声线→"Voice Strand", 音色→"Voice", 配方→"Recipe" (voice-mixing feature in `VoiceDesignerApp.tsx`), 自由活动→"Free Roam" (Xiaohongshu free-roam feature).

**Correction note**: "Mindscape" was coined for 心象 in the `apps/MusicApp.tsx` batch (2026-09-02) as a plausible-sounding English rendering, without checking whether the codebase already had an established English identity for this feature. It did not — it was never used in any translated file before this correction, so there was no real inconsistency to fix, just a stale reminder note. During batch 6 (`components/chat/`), multiple agents independently discovered that `components/chat/MessageItem.tsx`'s `THINKING_CHAIN_PRESETS` already pairs every preset's `titleZh: '心象'` with a literal `titleEn: 'PSYCHE'` field, rendered together as intentional bilingual card branding — plus pervasive code-level identifiers confirm this (`.sully-psyche`/`.sully-psyche-card/-title/-preview/-body` CSS classes, `PSYCHE_SELECTOR_REGEX`, `psycheSelector` variable, `'psyche-css'` protocol kind, used across `MessageItem.tsx`, `ThinkingChainSettingsModal.tsx`, `features/collaboration/makers.ts`, `types.ts`, `utils/chatFineTuneCss.test.ts`). **Use "Psyche" everywhere 心象 appears as prose/UI copy** (e.g. "Psyche · Settings", "Psyche card") — this is now used consistently in `MessageItem.tsx`, `ThinkingChainSettingsModal.tsx`, `ChatFineTunePanel.tsx`. **Update 2026-09-04**: `apps/Chat.tsx` is now translated — 心象 doesn't actually occur there as prose (only `.sully-psyche-*` CSS class references and "Psyche card" modal labels, already correctly using "Psyche"). Also, `apps/Character.tsx` (translated same batch) turned out to have zero occurrences of 心象 at all, despite an earlier assumption it would need this term — that assumption was simply wrong, not a bug. Still Chinese in **`apps/CallApp.tsx`** (not yet translated, the last remaining HIGH-risk file) — use "Psyche" there too if it appears, when its turn comes up.

**Batch 6 terms** (`components/date/`, 2026-09-02): 立绘模式→"Visual Mode", 阅读模式→"Reading Mode", 布置场景→"Scene Settings", 观测·开/关→"Observe · On"/"Observe · Off", 见面语音→"Date Voice", 加载更早见面记录→"Load Earlier Date History", 多选/多选删除→"Multi-select" (unified wording for one feature), 重播对话→"Replaying dialogue", OBSERVE_STYLES names 全息→"Hologram", 水墨→"Ink Wash", 霓虹→"Neon", 水晶→"Crystal", 终端→"Terminal" (plus the ink theme's `headerLabel` 观 · 录→"OBSERVE · RECORD"), "Portrait" as the standing translation for 立绘 (reuses the existing "Date Portrait" mapping — e.g. "Base Emotion Portraits", "Skin Sets"), 观测协议→"Observation Protocol" (the feature's descriptive name, vs. the bare "OBSERVE" wire-format/brand token), "Light Reading Mode", "Show Avatars in Reading Mode", "Writing Style & Narrative", "Detail Digging Guidance", "Panel Style", "Additional Dimensions".

**Batch 6 terms** (`components/date/story/`, 2026-09-02): 推进→"Advance" (user's turn/send-button label), 正文→"Story text" (AI-generated continuation), 楼层→"Layer", 事件盒→"Event Box", 幕后暗格→"Behind the Scenes", 尚未到账→"Not Yet Settled", 幕间频道→"Intermission Channel".

**Batch 6 terms** (`components/chat/`, 2026-09-02): 心象→"Psyche" (see the Glossary correction note above — supersedes the earlier "Mindscape" coinage). Thinking-chain card style names: Whisper, Minimal, Ink, Neuro, Kernel, Stellar, Tama Pet, Pixel, Muji, Custom (alongside the original Echo/PSYCHE preset). 防穿帮闸→"immersion-break guard". Archive prompt presets: Rational Refinement, Diary Style. Schedule styles: Lifestyle, Mindful (matched to existing internal `scheduleStyle` ids, not newly coined). Memory Link, Notification Sound. MCP order-card features: McDonald's / "End McDonald's Request", "Grab a Luckin" / "End Luckin Order" (瑞一杯/结束瑞一杯). Xiaohongshu kept as the brand name (not translated further, consistent with Luckin/McDonald's precedent). ChromeCssEditor.tsx CSS preset names: Cream Sweetheart, Neon Night, Minty Cream, Twilight Purple, Minimal White, Lavender Plush, Japanese Hot Spring. ChatConstants.ts preset `.name` labels: Atmosphere Monthly Journal, Key Points Digest (plus Rational Refinement / Diary Style listed above). Qixi card fallback title: "Starlit Dream Fairytale".

## Still open / not yet done

Single reference point for every "producer not yet translated" / "flagged, not fixed" gap found so far, so nothing gets lost across the Progress log. **Update this section** every time a new gap like this is found, or an existing one gets resolved (move it out, don't just leave it).

**RESOLVED 2026-09-04**: the `[html]...[/html]` wire-format tag found in `apps/GroupChat.tsx`'s `triggerDirector` HTML-mode prompt extension has been traced. Producer: `utils/htmlPrompt.ts` (already fully protected/confirmed). Consumer for 1:1 chat: `utils/applyAssistantPostProcessing.ts` (`5. [html]...[/html] → html_card`). Consumer for group chat: `utils/groupChat/dispatch.ts`. Both still out of scope, but the tag itself is now fully documented — add it to the main protocol list in "## Two categories of Chinese text" next time that section is touched.

**Producer files still in Chinese, whose constants now render inside already-translated (English) UI:**
- `utils/lifeRecords.ts` — genuine protocol file (defines the real `[[LIFE:PERIOD_START]]`/`[[LIFE:PERIOD_END]]` wire-format tags, out of scope for casual translation). Its `medFreqLabel()` function renders Chinese frequency text ("每天"/"隔天") inside the now-English `components/lifeRecord/LifeRecordPanel.tsx`.
- `utils/creatorCategories.ts` — `CC_CATEGORIES[].label` (e.g. "肤色"/"眼睛") renders Chinese inside the now-English `CreatorPartsUploader.tsx`.
- `utils/trackerSeeds.ts` — `TRACKER_TEMPLATES[].name`/`.blurb` renders Chinese inside the now-English `TrackerCreateSheet.tsx` (`components/handbook/`).
- `utils/amsgDiagnostics.ts` — `diagnosticRows`/`INSTANT_CHAT_BLOCKER_HINTS`/`resolveInstantChatBlocker()` renders Chinese inside the now-English `ActiveMsgGlobalSettingsModal.tsx`; also causes the `trackEvent('Can Instant Chat Be Enabled', { result })` analytics property to carry **mixed English/Chinese values** until this file gets its own pass.
- `gotchiScheme.ts` — `SCHEMES[].name` (11 Chinese scheme names, e.g. 星云紫/奶油白) renders Chinese inside the now-English `MobileGameHome.tsx`/`TamagotchiHome.tsx` palette panels.
- `utils/worldbook.ts` — `WORLDBOOK_POSITION_LABELS`/`WORLDBOOK_ROLE_LABELS`/`WORLDBOOK_POSITION_DESCRIPTIONS` renders Chinese inside the now-English `apps/WorldbookApp.tsx` dropdowns; also imported by `apps/Character.tsx` (not yet translated, per the batch-7 `apps/` scouting report).
- `utils/worldHome/prompts.ts` — `NARRATIVE_STYLES[].name`/`.guide` renders Chinese inside the now-English `apps/WorldHomeApp.tsx`'s "Narrative Style" picker.

**Terminology to reuse when these specific not-yet-translated files reach their own batch** (already decided, don't re-litigate — just apply):
- `apps/Character.tsx:1480`'s toggle label (still Chinese) → use **"Life Record Injection"**, matching `LifeRecordPanel.tsx`'s footer copy.
- `apps/Settings.tsx` (still Chinese) → use **"Real-time Perception"** for 实时感知, matching `ActiveMsgGlobalSettingsModal.tsx`.
- `apps/CallApp.tsx` (still Chinese, last remaining HIGH-risk file) → use **"Psyche"** for 心象 if it appears, matching `MessageItem.tsx`/`ThinkingChainSettingsModal.tsx`/`ChatFineTunePanel.tsx`. (`apps/Chat.tsx` and `apps/Character.tsx` are now both translated — turned out 心象 barely/never occurs in either as prose, see the correction note above.)

**Permanently protected (not a gap — these are correct as-is, listed here only so they aren't mistaken for unfinished work):** `components/character/RoomPlatePanel.tsx`'s `TAG_ICONS` match arrays (literal Chinese substrings matched via `.includes()` against AI-assigned tags — must stay Chinese forever, not a translation target).

**Batch 7 terms** (low-risk `apps/` cluster, 2026-09-03): from `apps/DateApp.tsx`/`WorldHomeApp.tsx`/`StudyApp.tsx` — "Seed"/"Seeds Shelf" (伏笔/伏笔栏), "Bonds" (羁绊), "Practice Book" (练习册). **Needs a second look**: "Chronicle" was used for both 编年史 (sim-mode chapters) and, with the qualifier, "World Chronicle" for 世界纪事 (the episode timeline) — two distinct Chinese terms landed on similar-sounding English wording; revisit and disambiguate further if it causes confusion once both features are visible side by side.

From `apps/MemoryPalaceApp.tsx`: 全自动记忆→"Full-Auto Memory", 水位线→"Waterline", 期盼/窗台期盼→"Anticipation"/"Windowsill Anticipation(s)" (matches the existing `Anticipation` type name), 副 API→"Secondary API", 认知消化→"Cognitive Digestion", 认知参数→"Cognitive Parameters", 反刍倾向→"Rumination Tendency", 整合回忆→"Consolidated Memory" (事件盒→"Event Box" reused from existing Glossary entry, not re-coined).

From `apps/VRWorldApp.tsx`: 信号坠落处→"Signal Fall" (matches a bilingual subtitle already baked into the source), 书库→Library, 接入→Connect, 留言簿→Guestbook, 娱乐室→Rec Room, 邮局→Post Office, 信匣→Letter Box, 不开口的核心→"the quiet core" (彼方→Beyond reused from existing Glossary entry).

From `apps/Settings.tsx`: no new feature-name coinages (reused "Real-time Perception", "Fish Audio", "McDonald's"/"Grab a Luckin" from existing Glossary entries). `HOTNEWS_PLATFORM_OPTIONS` labels romanized as real-world platform/brand names (Weibo, Zhihu, Toutiao, Baidu Tieba, 36Kr, East Money, etc.) — not tracked as coined terminology, same treatment as the Luckin/McDonald's/Xiaohongshu precedent (real brand names, not translated further).

**`apps/CheckPhone.tsx` terms (2026-09-04):** Check Phone's internal sub-app labels (`logPrefix`/`appLabel`, shown as `pc.app` in phone_card UI and in `Today's Activity`): 聊天软件→"Chat App", 通讯录→"Contacts", 通话记录→"Call Log", 购物APP→"Shopping App" (short display form "Taobao", reusing the existing app-name mapping), 外卖APP→"Food Delivery App" (short display form "Food"), 朋友圈→"Moments". 智能体 (the "peek at their AI usage" sub-feature within Check Phone, distinct from the top-level `PersonaSim` app) → "Agent". `AI_SERVICES` tab names: AI 助手→"AI Assistant", 深度对话→"Deep Talk" (tagline calls it a "Confidant", for 树洞), 酒馆→"Tavern". Vendor display labels in `getVendorTheme`: 豆包→Doubao, 文心一言→"ERNIE Bot", 通义千问→Qwen (matches the file's own internal `'qwen'` key). Relationship-system UI: 了解→"Learned", 话题盒→"Topic box", 好感→"Affinity", 备注名/关系→"Alias / relationship".

**`apps/GroupChat.tsx`/`RoomApp.tsx`/`SongwritingApp.tsx`/smaller-cluster terms (2026-09-04):** "Life Fragments" (生活碎片, RoomApp.tsx sidebar), "Room Template" (样板房, matches existing code identifiers), "Co-writer" (搭档/创作伙伴 unified, SongwritingApp.tsx), "Songs We Wrote Together" (一起写的歌), "Lyric Notebook" (歌词手帖), "Dual Cover" (合影封面). Confirmed via grep: none of these files use the `"我:"`/`"对方:"` wire-format token from `utils/relationshipChat.ts` (only `CheckPhone.tsx` does, so far) — checked because `CheckPhone.tsx`'s batch note flagged it as worth checking in sibling files.

**`apps/ScheduleApp.tsx`/`apps/JournalApp.tsx` terms (2026-09-04):** 心愿单→"Wish List", (soft-theme eventLabel) 纪念日→"Anniversaries". `PAPER_STYLES[].name`: 白纸→Plain, 网格→Grid, 点阵→Dot, 横线→Lined, 夜空→"Night Sky", 少女→Blossom. Confirmed via grep: neither file uses the `[html]...[/html]` tag (from `GroupChat.tsx`'s still-unresolved find) or the `"我:"`/`"对方:"` wire-format token (from `CheckPhone.tsx`'s batch) — still only `CheckPhone.tsx` uses the latter, so far.

**`apps/DreamTheater.tsx`/`apps/PersonaSim.tsx` terms (2026-09-04):** 小小梦境 · 喵梦盲盒→"Little Dreams · Meow Dream Blind Box" (blind-box series title). 生活记录 (PersonaSim's sub-app, `LifeLog` component) → **"Life Log"** — note this is a *different* Chinese source term from the unrelated existing "Life Fragments" (生活碎片, `RoomApp.tsx`) and "Life Record"/"Life Record Injection" (生活记录注入, `LifeRecordPanel.tsx`/`lifeRecords.ts`) glossary entries — three distinct features that all translate a "生活记录/生活碎片" family of Chinese names; don't conflate them if a future batch touches any of the other two. PersonaSim mode/tone labels: 日常模拟/事件模拟→"Daily Simulation"/"Event Simulation", 你的存在感→"Your Presence" (的三档 默认/轻度/无你→Default/Light/None), 演出基调→"Performance Tone" (随心/致郁/黑色幽默/轻盈可爱→Freeform/Melancholic/Dark Humor/Light & Cute). **TA/ta pronoun reminder**: confirmed the standing rule ("TA → they/them, not he/she/it," from the `components/character/` batch) also covers files where the source spells the pronoun with Latin letters ("ta"/"TA") directly inside Chinese prose, not just literal Chinese-character usage — apply "they/them/their" there too, don't render it as literal "ta".

**HIGH-risk `apps/` cluster terms (2026-09-04):** ScheduleApp.tsx: "Wish List"/"Anniversaries" (心愿单/纪念日, soft theme labels). JournalApp.tsx: PAPER_STYLES — Plain/Grid/Dot/Lined/Night Sky/Blossom (白纸/网格/点阵/横线/夜空/少女). DreamTheater.tsx: "Little Dreams · Meow Dream Blind Box" (小小梦境 · 喵梦盲盒), 13 dream archetype names (e.g. 甜梦→"Sweet Dream"). PersonaSim.tsx: "Life Log" (生活记录, PersonaSim's own sub-app — distinct from the unrelated "Life Fragments"/"Life Record" terms elsewhere), Persona Simulation sub-labels (Daily/Event Simulation, Your Presence, Performance Tone: Freeform/Melancholic/Dark Humor/Light & Cute), app-name placeholders romanized per the Luckin/McDonald's precedent (WeChat, Baidu) vs. translated as generics (Alarm/Photos/Browser/Weather/Notes/Music/Search). GameApp.tsx: reused "TRPG"/"Neural Link", no new coinages. Character.tsx: "Design Voice", "System Voices"/"Cloned Voices"/"Generated Voices", "Classic Parameters"/"New Natural Parameters", "Plates"/"Figures" tabs. Chat.tsx: cognitive-digestion result-modal labels — Confusion Resolved, Trauma Deepened, Knowledge Internalized, Self-Insight, New Self-Confusion, User Understanding Synthesized, Worries from Looking Back, New Anticipation, Settled into a Room Plate, Anticipation Fulfilled, Anticipation Unmet, Faded.

**Wire-format token family, confirmed scope (2026-09-04)**: the `"我:"`/`"对方:"` literal speaker-prefix token (parsed by out-of-scope `utils/relationshipChat.ts`'s `parseTranscript()`/`serializeTurns()`, first found in `CheckPhone.tsx`) was explicitly checked for in every file translated since — `GroupChat.tsx`, `RoomApp.tsx`, `SongwritingApp.tsx`, `BankApp.tsx`, `ScheduleApp.tsx`, `JournalApp.tsx`, `DreamTheater.tsx`, `PersonaSim.tsx`, `GameApp.tsx`, `Character.tsx`, `Chat.tsx` — and found in **none of them**. `CheckPhone.tsx` remains the only confirmed user of this token. Still worth checking in `apps/CallApp.tsx` when its turn comes, since it's the last untranslated HIGH-risk file and shares some relationship/dialogue-adjacent surface area.

## Environment notes

- **CONFIRMED RECURRING BUG (3+ occurrences, 2026-09-01/02): large multi-line `Edit` tool calls sometimes silently substitute straight quotes (`"`/`'`) with curly/smart quotes (U+201C/U+201D/U+2018/U+2019) in the applied diff** — not something the translating agent typed, it happens in the tool call itself. This has broken JSX attribute delimiters (`className=”...”` → invalid syntax → build failure) in `QixiDemoEvent.tsx`, `apps/Appearance.tsx`, `apps/ThemeMaker.tsx`, and `apps/WorldbookApp.tsx` so far, always inside large/wide edits, never seen in small single-line edits. Root cause unconfirmed (encoding issue in Write/Edit tool call, terminal, or some intermediate layer — not the model authoring curly quotes on purpose). **Standing mitigation**: after any large multi-line edit, grep the touched file for `[\x{2018}\x{2019}\x{201C}\x{201D}]` and diff against what you intended — if found where a straight quote should be (especially as a JSX/HTML attribute delimiter), fix with a scoped find-replace (a `perl -CSD` substitution over the affected line range has worked well) rather than re-doing the whole edit. This is now a **confirmed, recurring, real risk**, not a one-off — treat the existing "smart quote sweep" step in every batch's verification checklist as load-bearing, not a formality.
- Working directory: `D:\Documents\Claude\SullyOS`, Windows, git bash (the Bash tool in this session maps to Git Bash / POSIX sh, not PowerShell).
- Package manager is **pnpm**, not npm/yarn (`pnpm-lock.yaml` present) — per upstream CLAUDE.md above: `pnpm install`, `pnpm vitest run` / `pnpm test:run`, `pnpm <script>`.
- Build: `pnpm run build` (runs `build:workers` via `scripts/build-workers.mjs` then `vite build`). Dev: `pnpm dev` (vite).
- **No `rg` (ripgrep) available in this environment's PATH** — the built-in Grep/Glob tools also failed in this session with an unrelated infra error (`Executable not found in $PATH: ...claude.exe`, unclear if transient). Fallback used: GNU `grep` (v3.0, git-bash-provided) with `-P` (PCRE) flag, which **does** support `\x{4e00}-\x{9fff}` Unicode-range escapes — confirmed working. Re-test whether the Grep/Glob tools work again before assuming this workaround is still needed in future sessions.
- Repo was cloned **shallow** (`--depth 1`) because a full clone kept stalling/timing out on this network (very slow, bursty download speed to GitHub, retried twice before switching to shallow). If full history is ever needed later, `git fetch --unshallow` (may hit the same slow-network issue).
- This working copy has no `node_modules/` yet (never ran `pnpm install`) — `pnpm run build`/tests won't work until dependencies are installed; do that before the first translate-and-build-verify cycle.